# コミットメッセージ案（2026-08-29・ステップ8＋通知UI改善＋レイアウト崩れ修正）

前回のコミット（ステップ5+6+7＋ビルド修正）以降の変更分です。今回も前回コミットが存在するため、
通常通り`git add`→`git commit`で問題ありません。

（このファイル自体はGitにコミットしない、チャット経由で渡すだけの補助ファイルです）

---

## コミットメッセージ

```
feat: 通知ベルを全プロジェクト横断に対応し、通知専用画面を新設

- 通知の判定を、選択中プロジェクトのtasksではなく、自分が参加している全プロジェクトの
  タスク（notificationTasks）ベースに変更。ログイン時・20秒ポーリング・タスクの
  作成/編集/削除/ステータス変更/承認操作/サンプルデータリセットの直後に取得し直す
- 通知アイテムをクリックした際、そのタスクが選択中でない別プロジェクトのものであれば、
  自動的にcurrentProjectIdをそのプロジェクトへ切り替えてから編集モーダルを開くようにした
- ヘッダーの通知ベルは直近6件＋プロジェクト名タグのプレビューに変更し、新規の通知専用画面
  （NotificationsView.tsx）を追加。全件を種類ごと（遅延中／当日締切／差し戻された／
  承認待ち）に4セクション分けして表示する。サイドバーには項目を追加せず、通知ベルの
  「すべて見る→」からのみ遷移する
- 共有のためNotificationItem型をApp.tsxからtypes/task.tsへ移動

feat: ヘッダーに選択中プロジェクト名を常時表示

- ヘッダーの画面タイトル横に、選択中プロジェクト名のバッジを追加（sm未満の画面幅では省略）

fix: サイドバーのメニュー並び順を変更

- 「プロジェクト管理」をメニュー最下部（「タスク一覧」の下）へ移動

fix: 「プロジェクト管理」タブがタブレット幅でレイアウト崩れする不具合を修正

- カードの列数切り替えを、ビューポート幅ベースのsm:/lg:から、KanbanBoard.tsxと同じ
  コンテナクエリ（@min-[Npx]:。App.tsxの<main>の@containerが基準）方式に変更。
  タブレットでサイドバーを開いた状態のとき、ビューポート幅の割に実際の表示領域が
  狭くなり、カード内のボタン行（削除／メンバー管理／編集／開く）の文字がはみ出して
  崩れていたのを解消した
- 念のためボタン行自体にもflex-wrapを追加し、それでも幅が足りない場合はボタンが
  潰れず2行に折り返すようにした
```

---

## 補足

- Supabase側の変更は今回ありません。
- `git add` / `git commit` / `git push`はユーザー側で実行してください
  （`feature/project-management`ブランチ上）。
