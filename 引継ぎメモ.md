# 引継ぎメモ（スレッド移行用）

新しいチャットスレッドで作業を再開するときに、まずこのファイルを読んでください。
プロジェクト全体のルール・設計は`CLAUDE.md`が指す各ドキュメントを参照し、ここには
「直近で何をしていたか」「次に何をやるか」だけを書きます。内容が古くなったら、
このファイルは都度上書きしてOKです（過去の変更履歴は`仕様書.md`のchangelogを参照）。

最終更新：2026-08-31（ゲストログイン機能を実装・動作確認済み。プロジェクト管理機能側の
残りは実退会テストの保留分のみ）

---

## 直近の対応：ゲストログイン機能（2026-08-31）

プロジェクト管理機能とは無関係の別トピック。ユーザーから「ポートフォリオサイト経由の訪問者が
会員登録なしでアプリを試せるようにしたい」という要望があり実装。**動作確認済み・コミット
プッシュ待ち**（コミットメッセージは`commit-message-2026-08-31-guest-login.md`）。

- Supabaseの匿名認証（`signInAnonymously`）を使用。ログイン画面（`Login.tsx`）に
  「ゲストとしてログイン」ボタンを追加し、ログイン後は`App.tsx`側でデモ用プロジェクト＋
  サンプルタスク5件を自動投入する（`seedGuestDemoData`）。ログアウトすると
  `delete_own_account()`経由でデモデータ・匿名アカウント自体が自動削除される
- 実装中に3つの不具合に遭遇し、いずれも解決済み：
  1. Supabaseダッシュボードの「Anonymous sign-ins」設定の保存漏れ（ユーザー側で対処）
  2. `handle_new_user`トリガーが匿名ユーザー（emailがNULL）に未対応で、`profiles`作成に
     失敗する不具合 → `supabase-migration-guest-login.sql`で修正・ユーザーが実行済み
  3. デモデータ自動投入とApp.tsx側のログイン検知（プロジェクト一覧取得）が競合し、
     新規プロジェクトが画面に反映されないことがあるレースコンディション → デモデータ投入の
     責務をLogin.tsxからApp.tsx側（`projectsLoaded`フラグで順序を保証）へ移して解消
  4. （ついでに発覚・修正）退会時、`delete_own_account()`で自分を削除した直後に
     `signOut()`を呼ぶと`user_not_found`エラーが出る、既存の退会機能にもあった不具合。
     `signOutAfterAccountDeletion`ヘルパーで対処（実害はなく、コンソールに403ログが
     残るのみ。ブラウザ標準のネットワークログのためアプリ側では完全には消せない）
- 詳細は`仕様書.md`6.1・8章、`認証・DB設計書.md`12章に反映済み
- **次にやること**：ユーザーが`git commit` / `git push`を実行

## 現在進行中のタスク：「プロジェクト管理」機能

- **状態**：要件定義完了。実装ステップ1（DBマイグレーション）・ステップ3（App.tsx/
  Sidebar.tsxのフロントエンド実装）・ステップ4（プロジェクト管理タブ：一覧・作成・編集）に加え、
  ステップ4完了後にユーザーから出た追加要望（プロジェクト削除、サイドバー導線改善、
  検索/ステータスタブ絞り込み、全角半角対応）もすべて実装・動作確認済み（2026-08-29）。
  **コミット・プッシュ済み**（`feature/project-management`ブランチ、2026-08-29）。
  さらにステップ5（メンバー管理・オーナー譲渡UI）・ステップ6（`TaskForm.tsx`の
  プロジェクト移動欄）も実装・動作確認済み（2026-08-29）。ステップ6の動作確認中に、
  無関係の別不具合（後述の20秒ポーリングのクロージャ不具合）を発見・修正済み。
  さらにステップ7（退会フローへのオーナー引き継ぎ組み込み）も実装済み。SQL
  （`delete_own_account()`への「自分1人だけのオーナープロジェクトを削除」ステップ追加）は
  ユーザーがSupabase側で実行済み。フロント側の「オーナー引き継ぎセクションが表示される」
  ところまではユーザーが画面表示を確認済みだが、**実際に退会を最後まで実行するテストは
  「大変なので後で」という理由で保留中**（ユーザー了承の上での意図的な保留。バグではない）。
  ステップ5・6・7＋ポーリング不具合修正＋ビルドエラー修正（`initialTasks`への`projectId`
  追加漏れ）は**コミット・プッシュ済み**（ユーザーが`npm run build`成功を確認後に実施）。
  さらにステップ8（通知ベルの全プロジェクト横断対応）も実装し、動作確認済み。実装直後に
  ユーザーから出た追加のUI改善要望（通知ドロップダウンのプレビュー化＋通知専用画面
  ＋ヘッダーの選択中プロジェクト名バッジ。詳細は下記）、サイドバーの「プロジェクト管理」を
  メニュー最下部へ移動、および「プロジェクト管理」タブをタブレット幅で見た際のカード内
  ボタン行のレイアウト崩れ修正（後述）も、**すべて実装・動作確認済み（2026-08-29）**。
  ステップ8＋UI改善一式＋レイアウト崩れ修正は**コミット・プッシュ済み**
  （`commit-message-2026-08-29-step8.md`の内容で実施）。
  さらにステップ9（動作確認・`仕様書.md` / `認証・DB設計書.md`への反映）も完了しました。
  `仕様書.md`は1〜9章全体を今回の機能に合わせて更新（新設ファイル・状態一覧・データモデル・
  6.11節新設・ファイル一覧・TODO章）、`認証・DB設計書.md`は新規11章
  （`projects`/`project_members`テーブル・RLS設計・2件のRLS不具合の記録・
  `transfer_project_ownership`関数・`delete_own_account`拡張）を追加しました。**未コミット**。
  **これでプロジェクト管理機能_要件定義書.md§9の10ステップのうち、実質的な残りはステップ7の
  実退会テスト（保留中）と、ステップ10（将来TODO・組織全体の管理者ロール）だけです。**
  **次にやることは①ここまで（ステップ9のドキュメント反映分）をコミットする、②あとはユーザーが
  実退会テストを行うタイミングで最終確認、というフェーズに入ります**
- **ステップ4完了後に発生し解決済みの不具合**：プロジェクト新規作成が
  「新しい行が行レベルセキュリティポリシーに違反しています」（RLS違反、42501）で
  失敗する不具合が発生した。原因はINSERT自体ではなく、`.insert().select().single()`の
  RETURNINGが、作成直後に走るAFTER INSERTトリガー（作成者をオーナーとしてproject_members
  へ登録する処理）の完了を待たずにSELECT用RLSポリシー（`projects_select_member`）の
  可視性チェックを受けてしまい、「作成した本人なのに、まだメンバー登録が完了して
  いないので見えない」と判定される、RLSの“鶏と卵”問題だった。`projects_select_member`に
  `auth.uid() = created_by`を許可条件として追加し解消
  （`supabase-migration-projects-select-fix.sql`）。**新スレッドで「INSERT直後に
  AFTER INSERTトリガーが別テーブルへ権限系の行を追加し、かつRETURNINGでその場で
  結果を返す」という組み合わせのRLSポリシーを書くときは、この鶏と卵問題を疑うこと**
  （メンバー管理機能でメンバー追加後に何か即座に返す処理を書く場合も要注意）
- **ステップ3の動作確認状況**：サイドバーのアコーディオン表示、既存プロジェクト
  （「（移行前タスク）」）を選択した際のダッシュボード／タスクボード／スケジュールへの
  反映、新規タスク作成（`project_id`必須化への対応）は確認済み。「プロジェクト未選択時の
  案内表示」は、テストに必要な「一度もプロジェクトを選択したことが無い状態」を
  再現しづらいため**未確認のまま**（実装はしてある。ブロッカーではないので保留でよい）
- **ローカルファイルの同期漏れを修正済み（2026-08-29、ステップ5着手前に発見・修正）**：
  ステップ1のRLS不具合修正後、`supabase-migration-projects.sql`の関数方式への修正版
  （`is_project_member`/`is_project_owner`経由＋`projects_select_member`への
  `auth.uid() = created_by`追加）と、履歴用の`supabase-migration-projects-rls-fix.sql`・
  `supabase-migration-projects-select-fix.sql`が、実は`task-dashboard`フォルダに一度も
  書き込まれておらず（前回セッションで保存先を`/tmp`と間違えていた）、フォルダ内の実ファイルは
  修正前の古い内容のままだった（Supabase側には適用済みで動作確認も済み。ズレていたのは
  ローカルファイルだけ）。ステップ5着手前に発見し、3ファイルとも最新版へ同期済み。
  **Supabase側は既に修正適用済みなので、これらのファイルを再実行する必要はない**。
  **新スレッドでは、ローカルの.sqlファイルとSupabase本番の実際の状態が食い違っていないか
  （特に前回セッションで直接SQL Editorで修正した場合）を都度疑うこと**
- **ステップ1で発生し解決済みの不具合**：初版の`supabase-migration-projects.sql`を実行した
  直後、既存タスクが画面から見えなくなる不具合が発生した。原因は`projects` /
  `project_members` / `tasks`のRLSポリシーが`project_members`を素朴なEXISTS句で
  自己参照しており、Postgresの「recursive RLS policy」の罠にかかっていたため。
  `is_project_member()` / `is_project_owner()`という`security definer`のヘルパー関数に
  判定ロジックを切り出す形に修正し（`supabase-migration-projects-rls-fix.sql`を追加実行）、
  タスクの表示が復旧したことを確認済み。`supabase-migration-projects.sql`本体も、将来
  別環境にゼロから流し直す場合に同じ不具合が起きないよう、関数方式に修正済み
  （実行済みの環境への影響は無し）。**新スレッドでRLSポリシーを追加・変更する際は、
  同じテーブルを素朴なEXISTSで自己参照せず、必ずsecurity definer関数を経由させること**
- **要件定義書**：`プロジェクト管理機能_要件定義書.md`（1〜9章。方針はすべて確定済み）
- **スコープ（確定）**：本格的な複数プロジェクト管理を導入する。`projects` /
  `project_members`テーブルを新設し、`tasks`に`project_id`を追加。ダッシュボード／
  タスクボード／スケジュールの3画面は作り直さず、`currentProjectId`という状態を
  `App.tsx`に1つ追加して絞り込むだけにする

### 確定した主要方針（要件定義書からの要点）

- UI/UXは2段構え：①サイドバーの「プロジェクト管理」項目をアコーディオン化して
  素早く切り替え（§2.1）、②「すべて管理→」から開く一覧ページでプロジェクトの
  作成・編集・メンバー管理を行う（§2.3〜2.4）
- プロジェクトのステータスは**「進行中」「完了」「アーカイブ」の3値**（2026-08-31に
  2値案から3値案へ修正済み。デフォルト非表示になるのはアーカイブのみ）
- タスクはプロジェクト間を移動できるようにする（§2.5）
- 通知は選択中プロジェクトに限定せず、参加している全プロジェクトを横断して通知する（§4）
- サンプルデータリセットはプロジェクト単位に変更せず、現状通りアプリ全体スコープのまま（§5）
- 退会（アカウント削除）時、自分がオーナーで他にメンバーがいるプロジェクトは、新しい
  オーナーを指定してから退会する。新設の`transfer_project_ownership()`関数
  （security definer）で実現する（§6）。自分1人だけのプロジェクトは退会と同時に削除
- ロール管理は「プロジェクト単位のオーナー／メンバー」のみを今回実装し、組織全体の
  管理者ロールは設計だけ§7.3に残してTODOにする（YAGNI判断。スキーマ上は
  `profiles.app_role`を後乗せするだけで対応できる拡張性を確保済み）

### まだ細部が決まっていない論点（実装しながら決めてよい粒度。§8参照）

1. プロジェクトカードの完了率の計算方法（`done`比率か`KpiCards.tsx`のロジック流用か）
2. アーカイブ済みプロジェクトの「表示する」トグルをどの画面に置くか
3. タスクを別プロジェクトへ移動した際、移動先のメンバーでなくなる担当者・確認者の扱い
   （自動クリアで仮決め。警告表示を出すかは未定）
4. メンバーが0人になったプロジェクトを自動アーカイブするかどうか

### 次にやること（実装ステップ。要件定義書§9より）

1. ✅ **完了**：`supabase-migration-projects.sql`を新規作成し、Supabase側で実行・
   動作確認済み（`projects` / `project_members`テーブル、プロジェクト作成時に自動で
   オーナー登録するトリガー、`transfer_project_ownership`関数、`tasks.project_id`列追加＋
   既存タスクの既定プロジェクトへの移行、RLSポリシー。§3・§3.4・§6.1・§7.1を1ファイルに
   統合）。実行直後に発生した再帰的RLSの不具合も`is_project_member`/`is_project_owner`関数
   経由への修正で解消済み（詳細は上記）
2. （ステップ1に統合済み。要件定義書§9の項目2はステップ1のファイルに含めた）
3. ✅ **完了**：サイドバーの「プロジェクト管理」をアコーディオン化し、
   `currentProjectId`を`App.tsx`に追加。ダッシュボード／タスクボード／スケジュールの
   問い合わせに`project_id`の絞り込みを追加。ビルド成功・基本動作確認済み（詳細は上記）
4. ✅ **完了**：プロジェクト管理タブ（`ProjectManagementView.tsx`：カード一覧・
   `ProjectFormModal.tsx`：新規作成／編集）を実装。作成・編集ともユーザーが動作確認済み。
   さらにステップ4完了後、ユーザーからの追加要望（下記参照）にも対応し、すべて
   動作確認済み：①プロジェクト削除機能（オーナーのみ、確認ダイアログあり）、
   ②サイドバーのアコーディオンで「＋新規プロジェクト」を先頭固定＋プロジェクト一覧を
   スクロール化、③「すべて管理」タブに検索ボックス＋ステータスタブ（すべて／進行中／
   完了／アーカイブ）を追加し旧「アーカイブ済みを表示」チェックボックスを廃止、
   ④検索の全角半角ゆらぎをNFKC正規化で吸収、⑤プロジェクト名入力欄に
   `autoComplete="off"`を追加（ブラウザの自動入力による誤表記混入の予防）。
   **ステップ1+3+4＋この追加分をまとめて1コミットにし、コミット・プッシュ済み（2026-08-29）**
5. ✅ **完了**：メンバー管理・オーナー譲渡UIを実装。新規`MemberManagementModal.tsx`
   （メンバー一覧・追加・削除・オーナー譲渡）、`ProjectManagementView.tsx`にオーナー向け
   「メンバー管理」ボタン・非オーナー向け「抜ける」ボタンを追加。DB側は
   `transfer_project_ownership()`が既に用意済みだったため新規SQLは不要（RPC呼び出しのみ）。
   ユーザーが動作確認済み（メンバー追加・削除、オーナー譲渡後のボタン表示切替、
   メンバーの脱退）。**未コミット**
6. ✅ **完了**：`TaskForm.tsx`にプロジェクト移動欄を追加（§2.5）。編集画面にのみ
   「プロジェクト」欄を表示し、自分が参加している他のプロジェクトへ移動できる。あわせて、
   作業担当者・確認者の選択候補を「選択中プロジェクトのメンバーのみ」に絞り込むよう変更
   （プロジェクト機能導入前は全登録ユーザーが対象だった）。プロジェクトを変更すると、
   移動先のメンバーでなくなる担当者・確認者だけを自動的に選択から外す（全リセットではない）。
   この絞り込みに使う`projectMembers`は、ログイン時点で取得するよう変更（元は「プロジェクト
   管理」タブを開いたときだけ取得していた）。ユーザーが動作確認済み。**未コミット**
   - **ステップ6の動作確認中に発見・修正した、無関係の別不具合**：タスク一覧の20秒おき
     ポーリング（`setInterval`。通知ベル用）が、依存配列`[isAuthenticated]`のみだったため、
     クロージャがタイマー作成時点の`currentProjectId`を握ったまま更新されず、プロジェクトを
     切り替えても「ログイン直後に選択されていたプロジェクト」のタスクを取得し続けていた
     （切り替え直後は正しく表示されるが、最大20秒以内に別プロジェクトの内容で上書きされる、
     という形で発現）。依存配列に`currentProjectId`を追加し解消（`App.tsx`）。
     **新スレッドでは、setInterval/setTimeoutのコールバックが依存配列に無い状態変数を
     参照していないか（クロージャの陳腐化）を疑うこと**
7. ✅ **完了（実退会テストのみ保留）**：退会フローにオーナー引き継ぎのステップを追加
   （要件定義書§6.2）。新規`OwnershipHandoverSection.tsx`（プロジェクトごとに新オーナーを
   選ぶプルダウン＋「譲渡する」ボタン）を追加し、`SettingsView.tsx`「データ」タブで、
   自分がオーナーかつ他にもメンバーがいるプロジェクトが1件以上ある間は通常の退会ボタン
   （`DangerZoneSection.tsx`）の代わりにこちらを表示する。譲渡が完了して対象0件になると、
   `App.tsx`側の派生値`projectsNeedingOwnershipHandover`が再計算され、自動的に通常の
   退会ボタンへ切り替わる（明示的な画面遷移stateは持たせていない）。退会実行用の
   `delete_own_account()`（`supabase-migration-account-deletion.sql`）にも、「自分がオーナーで
   他に誰もメンバーがいないプロジェクトをタスクごと削除する」ステップを追加し
   （`supabase-migration-account-deletion-owner-handover.sql`）、**ユーザーがSupabase側で
   実行済み**。`DangerZoneSection.tsx`の警告文言にも、自分1人だけのオーナープロジェクトが
   削除される旨を追記。**動作確認状況**：オーナー引き継ぎセクションが正しく表示されることは
   ユーザーが確認済み。ただし、①実際に譲渡ボタンを押して完了させる、②退会を最後まで実行して
   自分1人だけのプロジェクトが実際に削除されることの確認、の2点は「テストが大変なので後で」
   という理由でユーザーが意図的に保留中（**新スレッドで引き継ぐ場合、この2点は完了扱いに
   しないこと**。バグの疑いがあるわけではなく、退会という取り返しのつかない操作の
   テストコストが理由）。**未コミット**
8. ✅ **完了（追加UI改善・レイアウト崩れ修正込み、すべて動作確認済み）**：通知ベルを
   全プロジェクト横断に対応（§4）。`notificationTasks`という判定専用stateを新設し、
   project_idで絞り込まず全タスクを取得（RLSで自動的に自分の参加プロジェクト分だけ返る）。
   通知アイテムをクリックした際、選択中でない別プロジェクトのタスクなら自動的に
   `currentProjectId`を切り替えてから編集モーダルを開く（`handleNotificationClick`）。
   - **直後にユーザーから出た追加UI改善要望（2026-08-29、実装・動作確認済み）**：
     ①全プロジェクト横断になって「どのプロジェクトの通知か分からない」「件数が増えると
     ヘッダーの小さいドロップダウンだけでは窮屈」という指摘を受け、ヘッダーの通知ベルは
     直近6件＋プロジェクト名タグのプレビューに変更し、新規の通知専用画面
     （`src/components/notifications/NotificationsView.tsx`・`currentView='notifications'`。
     サイドバーには項目を追加せず、設定ページと同様ベルの「すべて見る→」からのみ入る）に
     全件を種類ごと4セクション分けで表示するようにした。共有のため`NotificationItem`型を
     `App.tsx`から`types/task.ts`へ移動。②「今どのプロジェクトを見ているか常に分かるように
     したい」との要望で、ヘッダーに選択中プロジェクト名の常時表示バッジを追加
     （`App.tsx`。sm未満の画面幅では省略）。③将来、タスク内のコメント／チャットのような
     やり取り機能を追加し、それをきっかけに通知の種類を増やしたいという構想が出た
     （**TODO・未着手**。今回はYAGNI判断で作り込まず、通知の種類が増えても
     `NotificationsView.tsx`の`TYPE_SECTIONS`配列への追加だけで対応できる設計にとどめてある。
     チャット機能自体の要件定義はまだ行っていない。詳細は下記「未着手の細かい積み残し」参照）
   - **サイドバーのメニュー並び順変更（2026-08-29、ユーザー要望・実装済み）**：
     「プロジェクト管理」をメニュー最下部（「タスク一覧」の下）へ移動
     （`Sidebar.tsx`の`menuItems`配列の並び順のみの変更）
   - **「プロジェクト管理」タブのタブレットレイアウト崩れ修正（2026-08-29、ユーザー報告・
     修正済み）**：タブレット幅でサイドバーを開いた状態のとき、カード内のボタン行
     （削除／メンバー管理／編集／開く）の文字がはみ出して崩れる不具合を発見・修正した。
     原因はKanbanBoard.tsxのカラム幅で以前直したのと同じパターンで、カードの列数切り替えを
     ビューポート幅ベースの`sm:`/`lg:`で判定していたため、「ビューポート的には2〜3列に
     見えるはずの幅だが、サイドバー分を引いた実際の表示領域はもっと狭い」というズレが
     起きていた。`ProjectManagementView.tsx`のグリッドを、KanbanBoard.tsxと同じ
     `@min-[Npx]:`（App.tsxの`<main>`の`@container`基準）方式に変更して解消。念のため
     ボタン行自体にも`flex-wrap`を追加し、それでも狭い場合はボタンが潰れず2行に折り返す
     ようにした。**新スレッドでは、ビューポート幅ベースのsm:/md:/lg:をタブレット幅の
     レイアウト崩れの調査対象にした場合、この`@container`パターンを疑うこと**
     （サイドバーが開閉するレイアウトでは、ビューポート幅と実際の表示領域が一致しないため）
9. ✅ **完了**：動作確認・`仕様書.md` / `認証・DB設計書.md`への反映。`仕様書.md`は
   1章（概要）・3章（ディレクトリ構成）・4章（状態管理）・5章（データモデル：Project型追加）・
   6章（6.2通知ベル刷新・6.3サイドバー・6.7 TaskForm・6.9設定ページ・6.10タブレット対応・
   6.11プロジェクト管理機能の節を新設）・7章（ファイル一覧）・8章（TODO）・changelogを更新。
   `認証・DB設計書.md`は新規11章を追加し、`projects`/`project_members`テーブル・
   `tasks.project_id`・RLS設計・実装時に発生した2件のRLS不具合（再帰的RLS／INSERT直後の
   RETURNINGとの鶏と卵問題）とその対処・`transfer_project_ownership`関数・
   `delete_own_account`の拡張をまとめて記録した。0章の「プロジェクト管理は対象外」という
   古い記述も、実装済みである旨に更新した。**未コミット**
10. （将来・TODO）組織全体の管理者ロールが本当に必要になったタイミングで着手

**作業ブランチ**：`feature/project-management`（ユーザーがローカルで`git checkout -b`実行済み）。
ステップ1+3+4＋追加分（削除・サイドバーUX・検索絞り込み等）を1コミットにまとめてコミット・
プッシュ済み（2026-08-29）。このセッションはユーザーのPC上でgitコマンドを直接実行できないため、
`git add` / `git commit` / `git push`はユーザー側で行う運用

#### ユーザーからの追加要望（2026-08-29、すべて対応・動作確認済み）

- **プロジェクト自体の削除機能**：`ProjectManagementView.tsx`のカードにオーナーのみ
  表示される削除ボタンを追加。`App.tsx`の`handleDeleteProject`で確認ダイアログ
  （タスクも一緒に削除される旨を明示）→`projects`テーブルへのdelete、を実装
- **サイドバーのアコーディオンUI改善**：「＋新規プロジェクト」をリストの先頭に固定表示し、
  プロジェクト一覧は`max-h-40 overflow-y-auto`でスクロール化（`Sidebar.tsx`）
- **「すべて管理」タブへの検索/絞り込み追加**：検索ボックス（プロジェクト名の部分一致）＋
  ステータスタブ（すべて／進行中／完了／アーカイブ）を追加し、旧
  「アーカイブ済みを表示」チェックボックスは廃止（`ProjectManagementView.tsx`）
- **検索の全角/半角ゆらぎ対応**：検索比較の直前にのみ`.normalize('NFKC')`を適用（保存・
  表示内容は変更しない）。ひらがな⇔カタカナの違いまでは同一視しない範囲
- **プロジェクト名の表記ズレ報告への対応**：コード側に文字列変換処理は無く、
  ブラウザの自動入力（オートコンプリート）が原因の可能性が高いと判断。予防策として
  `ProjectFormModal.tsx`のプロジェクト名入力欄に`autoComplete="off"`を追加

#### ステップ1で追加した、要件定義書に明記の無い実装判断（要確認の上ユーザー承認済み・2026-08-29）

- 既定プロジェクト「（移行前タスク）」の`created_by`（＝初期オーナー）は、**一番古く
  登録したユーザー**とした（要件定義書に定めが無かったため）。あくまで暫定値で、
  移行後にプロジェクト名変更・タスクの再配分はユーザーが自由に行える前提（§3.4）
- `tasks.project_id`に`on delete cascade`を付与（§6.2で将来必要になる「オーナー1人だけの
  プロジェクトを退会と同時に削除」を見据えた先回り対応）
- `idx_project_members_user_id` / `idx_tasks_project_id`索引を追加（既存の
  `idx_tasks_end_date`と同じ、頻出クエリの高速化目的）
- `project_members`の削除ポリシーに「`role='owner'`の行は本人でも直接削除できない」制限を
  追加（§7.4をDB側でも保証。オーナー交代は`transfer_project_ownership()`経由のみ）
- 要件定義書§9のステップ1・2は、既存マイグレーションファイルと同じ「1機能=1ファイル」の
  粒度に合わせて`supabase-migration-projects.sql`1本に統合

---

## 直近セッションでの主な作業（サマリ）

このスレッドで完了した作業（詳細は`仕様書.md`のchangelog参照）：

- ドキュメント整理：`仕様書.md`を業務用に整理し、初心者向け解説を`学習ノート.md`として
  新規分離。`認証・DB設計書.md`の文言整理、`規約.md`のアプリ名表記修正
- UI修正3件：①「アプリについて」タブのW+ロゴをSidebar/Loginと統一（角丸四角）、
  ②アバター画像クリックで原寸表示できる`ImageLightbox`コンポーネントを追加、
  ③アクセントカラー背景の文字色を`--theme-on-accent`変数で可読性改善
  （最終的にダーク系＝黒／ライト系＝白の単純な2分岐に統一。W+ロゴは固定黒に戻した）
- レイアウト修正：サイドバー展開時のタブレット幅でカンバン／スケジュールが崩れる不具合を、
  コンテナクエリ（`@min-[Npx]:`）ベースに変更して解消
- ラベル文言修正：「確認者の承認待ち」→「承認待ち」
- 「プロジェクト管理」機能の要件定義書を新規作成し、ユーザーからの6つの回答を反映して
  UI/UX要件（アコーディオン方式・カード一覧）を具体化。ステータスの値は
  いったん2値案として反映してしまい、後から3値案（進行中／完了／アーカイブ）に訂正した
- `CLAUDE.md`を整理：頻繁に変わらない事実だけを残し（`規約.md`を`@`インポート）、
  ファイル種別ごとの詳細ルールを`.claude/rules/`（YAMLの`paths`で対象パスを指定する
  パス固有ルール）に分離。ブランチ運用・コミットメッセージ規約も追加（提案ベース）。
  コンテキスト使用率50%程度を目安に`/compact`かスレッド移行を検討する運用ルールも追加
- `README.md`を全面的に書き直し（プロジェクト概要・機能・技術スタック・セットアップ手順・
  テスト/CIの説明・ドキュメントへのリンクを整備）
- 未使用ファイルの整理：`App.css` `src/components/SettingsModal.tsx`を削除（ユーザーが
  実施）。`src/assets/`の3ファイル（react.svg / vite.svg / hero.png）は、画像を使う
  可能性があるため保持
- テスト基盤を新規導入（Vitest + Testing Library）：`src/utils/date.test.ts`
  `src/utils/assignee.test.ts`（日付計算・タスク絞り込みのユニットテスト）、
  `src/components/kanban/TaskCard.test.tsx`（規約③「遅延最優先ルール」の回帰テスト。
  差し戻し中かつ期日超過の場合に遅延の枠線スタイルが優先されることを確認）。
  `npm run test`で実行。テスト対象ファイルは`tsconfig.app.json`の`exclude`で
  本番ビルドの型チェック対象から除外し、テスト側の問題でビルドが止まらないようにした
- 導入後に実際に`npm run test`してもらったところ2件の不具合が判明し、修正済み：
  ①`TaskCard.test.tsx`で`React`のimport漏れによる`ReferenceError` → import追加で解消、
  ②`vitest.config.ts`で`globals: true`を使っていないため、Testing Libraryの自動DOM
  cleanupが効かず前のテストの描画結果が残ってしまう問題 → `src/test/setup.ts`に
  `afterEach(cleanup)`を明示的に追加して解消。さらに、テスト対象ファイルを
  `tsconfig.app.json`から除外した副作用でエディタ上`toBeInTheDocument`等が型エラー表示に
  なっていたのを、`tsconfig.test.json`を新設して解消（本番ビルドの型チェックからは
  引き続き独立）。最後に`npm run build`で`vitest`と`vite`（8系・Rolldown）のPlugin型不整合
  エラーが出たため、`vitest`をVite 8対応の4.1系に更新して解消。
  **`npm run test` / `npm run build`とも成功をユーザーに確認済み（2026-08-31）**。
  `.github/workflows/ci.yml`もユーザーが配置済みなので、あとはpushすればCIが動く状態
- GitHub ActionsでCIを新規追加（`.github/workflows/ci.yml`）：`main`へのpush・PRごとに
  `lint` → `test` → `build`を自動実行
- `仕様書.md`を上記の変更に合わせて更新（changelog・技術スタック表・ディレクトリ構成・
  ファイル一覧・新設した7.1テスト/7.2 CI節・8章TODO）

## 保留中（ユーザーの意向確認済み）

- **PreToolUseフックでのルール強制**：`.env`コミット防止など、CLAUDE.md/規約.mdの
  「読んでもらう」形の指示ではなく機械的にブロックしたいものが出てきたら、ユーザーから
  依頼がある想定（現時点では未着手でよい、という合意）

## 未着手の細かい積み残し（優先度低）

- 担当者・確認者選択を検索式のUIにする案（未着手）
- Supabase Realtimeの導入（未着手。2026-08-30、20秒ポーリングのかくつき対策として
  一度検討したが、費用面は無料枠に収まる見込みと確認した上で、設計・実装コストの
  大きさから今回は軽量な対策のみに留め、本格導入は引き続きTODO扱いとした）
- 上記「プロジェクト管理」要件定義書§8の4つの細部論点
- **タスク内のコメント／チャットのようなやり取り機能（2026-08-29、ステップ8のUI改善時に
  ユーザーから出た構想。TODO・未着手）**：まだ要件定義は行っていない。実現した場合、
  新しい通知の種類（例：「タスクにコメントが付いた」）が増えることが見込まれるため、
  通知ベルの実装（`NotificationType`・`NotificationsView.tsx`の`TYPE_SECTIONS`・
  `SettingsView.tsx`のON/OFF設定）は種類を追加しやすい設計にしてある
- **放置されたゲスト（匿名）アカウントの定期削除（2026-08-31、未着手）**：ゲストアカウントは
  ログアウト時に自動削除されるが、ログアウトせずブラウザを閉じただけの場合はDB上に残り続ける。
  一定期間操作のない匿名アカウントを定期的に一括削除する仕組み（バッチ処理等）はYAGNI判断で
  見送った（`認証・DB設計書.md`12.6参照）

## 運用上の注意

- コミットメッセージは`commit-message-*.md`のように日付付きで作成し、チャットでも
  同じ内容を提示する運用。このファイル自体はGitリポジトリにコミットしない想定
  （チャット経由で都度渡すだけの補助ファイル）
- **各ステップ／まとまった変更が一段落するたびに、ユーザーに聞かれなくてもコミットの
  タイミングかどうかをこちらから確認・提案する**（2026-08-29にユーザーから明示的に
  依頼された運用。毎回言わせない）
- このサンドボックスは`npm install` / `npm run build` / `npm run test`を実行できないため、
  動作確認はユーザーがローカルで行い、エラーがあれば貼ってもらって対応する
  （GitHub Actions CIを追加したので、push後の自動確認はある程度カバーされる）
- **コミットメッセージを複数に分割提案する際の注意**：直前のコミットが無い（＝まだ一度も
  コミットしていない）状態のブランチでは、同じファイルに複数回の変更が地続きで
  積み重なっていても、`git add`はファイルの「今の中身全部」しかステージできず、
  変更を複数コミットへきれいに分割することはできない（`git add -p`でのハンク単位選別は
  非現実的）。この状況で複数コミットを提案すると同じファイルが両方に出てきて矛盾する
  （2026-08-29に実際にやってしまい、ユーザーから指摘を受けて1コミットへ訂正した）。
  **直前のコミットの有無を確認しないまま複数コミットに分割提案しないこと**。また、
  このセッションはユーザーのPC上で`git status`/`git diff`を実行できないため、対象
  ファイル一覧を正確に列挙するのは避け、「変更されたファイルをまとめてコミットして
  ください」という形に留めるのが安全
