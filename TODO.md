# TODO

未着手・保留中のタスク一覧です。「今後直す予定」ではなく恒久的な仕様・制約については
`docs/基本設計書.md`9章「既知の制約・設計上の境界」を参照してください。

最終更新：2026-09-02（`docs/`ドキュメント再編にあわせて`仕様書.md`8章から抽出）

## 未確認・要確認

- **Supabase実装のうち、実機での動作確認がまだ済んでいない部分がある**：サインアップ・
  サインイン・タスクの新規作成は実機で確認済みだが、タスクの編集・削除・承認フロー
  （申請／承認／差し戻し）・通知ベル・サブタスクのSupabase連携・設定ページのサンプルデータ
  リセットは、今後順次確認していく予定
- **プロフィール・退会機能用の追加マイグレーションが未実行の可能性がある**：
  `supabase-migration-profile.sql`（アバター列・Storageバケット）と
  `supabase-migration-account-deletion.sql`（`delete_own_account`関数）は、Supabase側の
  SQL Editorで実行済みかどうかを別途確認する必要がある。未実行の場合、アバター
  アップロードと退会機能は失敗する
- **退会×オーナー引き継ぎの実退会テストが保留中**：オーナー引き継ぎセクションが正しく
  表示されることは確認済みだが、①実際に譲渡ボタンを押して完了させる、②退会を最後まで
  実行して自分1人だけのプロジェクトが実際に削除されることの確認、の2点は「テストが大変
  なので後で」という理由で意図的に保留中（バグではない）

## 未着手・構想段階

- **Supabase Realtime対応**：他ユーザーの操作がリアルタイムで画面に反映される機能。
  「任意」と位置づけており、明示的な着手指示が出るまでは着手しない
- **タスク内のコメント／チャットのようなやり取り機能**：構想段階、まだ要件定義は
  行っていない。実現した場合、新しい通知の種類が増えることが見込まれるため、通知ベルの
  実装（`NotificationType`・`NotificationsView.tsx`の`TYPE_SECTIONS`・`SettingsView.tsx`の
  ON/OFF設定）は種類を追加しやすい設計にとどめてある
- **組織全体の管理者ロール**：プロジェクト単位のオーナー／メンバーロールは実装済みだが、
  組織全体を横断する管理者ロールはYAGNI判断で見送り（`要件定義書_プロジェクト管理機能.md`
  §7.3。拡張は後乗せ可能な設計にしてある）
- **放置されたゲスト（匿名）アカウントの定期削除**：ゲストアカウントはログアウト時に自動
  削除される設計だが、ログアウトせずブラウザを閉じただけの場合はDB上に残り続ける。一定期間
  操作のない匿名アカウントを定期的に一括削除する仕組み（バッチ処理等）はYAGNI判断で見送り

## 既知の制約（今回は据え置き）

- **担当者を全員選ぶと確認者の候補が0人になる**：`TaskForm.tsx`の確認者（レビュアー）候補は
  「担当者に選ばれていないユーザー」から絞り込む仕組みのため、登録ユーザーが少ないうち
  （特に自分1人だけの場合）は確認者の選択肢が無くなる。`UserPicker.tsx`のドロップダウンに
  「選べる候補がいません」という案内文は出しているが、制約自体は緩和していない

## 軽微・削除候補

- **`KpiCards.tsx` の `totalTasksCount` propが未使用**：`DashboardView.tsx`から渡されているが、
  `KpiCards.tsx`内で実際には使われていない。削除候補
