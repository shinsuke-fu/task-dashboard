# CLAUDE.md — WORK PLUS 開発ガイド

このリポジトリで作業するAIアシスタント（Claude）が、セッション開始時に読む入口ファイルです。
「毎回変わらない事実・ルール」だけをここに置き、頻繁に変わる状況（今何をやっているか）は
`引継ぎメモ.md`に、ファイル種別ごとの詳細ルールは`.claude/rules/`に分けています
（理由は本ファイル末尾「ドキュメント構成の方針」参照）。

@規約.md

## プロジェクト概要

- **名称**：WORK PLUS（ロゴ表記「W+」）。React 19 + TypeScript + Vite + Tailwind CSS v4の
  一面集約型SPA。認証・DB・ストレージはSupabase
- **目的**：フロントエンド職の転職活動（日本）向けポートフォリオ作品
- **開発機**：Windows PC（`C:\Users\shin\Desktop\project\task-dashboard`）。Claude
  （Cowork）のデバイスブリッジ経由で作業することが多い

## 新しいスレッドで最初にやること

1. **`引継ぎメモ.md`を読む** — 直近何をしていたか、次に何をやる予定かが書いてある
2. 触るファイルに応じて`.claude/rules/`のルールが自動で読み込まれる（下記参照）ので、
   個別に探しに行く必要は基本的にない
3. 機能追加・DB変更に着手する前に、関係するドキュメントを読む（下記「必要になったら読む
   ドキュメント」）

## コンテキストの軽量化（compact／スレッド分割の目安）

- 会話のコンテキスト使用率が**50%程度**に達したら、たまったまま作業を続けず、
  いったん軽量化するかどうかを検討するのが目安
- **同じ作業の続きなら`/compact`**：`/compact 直前の変更内容に絞って`のように焦点を
  指定すると、要約で残す内容の精度が上がる
- **別の機能・別テーマに切り替えるなら、compactより新しいスレッドへの移行を優先**：
  スレッドをまたぐ場合は、移行前に必ず`引継ぎメモ.md`を最新化しておく（このファイルは
  CLAUDE.mdから自動では読み込まれないため、更新しないまま移行すると引き継げない）
- 大きなドキュメント（`仕様書.md` `認証・DB設計書.md`等）は毎回全文を読み直さず、
  必要なセクションだけを確認するなど、そもそも消費を抑える読み方を心がける

## 必要になったら読むドキュメント（常時は読み込まない）

| ファイル | 読むタイミング |
|---|---|
| `仕様書.md` | 既存の画面仕様・データモデル・ディレクトリ構成を確認したいとき |
| `認証・DB設計書.md` | Supabaseのテーブル・RLS・security definer関数を変更／参照するとき |
| `プロジェクト管理機能_要件定義書.md` | 「プロジェクト管理」機能に着手・関連する変更をするとき |
| `学習ノート.md` | ユーザー向けの初心者説明のトーンを合わせたいとき（AIの実装作業には不要） |

## ビルド・確認コマンド

- このクラウド上のサンドボックスでは`npm install` / `npm run build` / `tsc`を実行できない。
  コード変更後、ビルド・型チェックはユーザーがローカルで`npm run build`を実行して確認する
  （ローカルのClaude Codeとして起動している場合は、通常通り実行してよい）
- Lintは`.oxlintrc.json`（oxlint）を使用

## 命名・ディレクトリ規約（既存コードから）

- コンポーネント：`PascalCase.tsx`。機能単位で`src/components/<feature>/`配下に置く
  （`dashboard/` `kanban/` `schedule/` `settings/`）。複数機能で共有するものだけ
  `src/components/`直下（`Sidebar.tsx` `ImageLightbox.tsx`等）
- 画面（ルーティング相当）：`src/pages/`（`Login.tsx` `ResetPassword.tsx`）
- ロジック・設定：`camelCase.ts` / `lowercase.ts`（`src/utils/` `src/lib/` `src/types/`
  `src/constants/`）。1ファイル1関心を維持する
- 新しくカスタムフックを作る場合は`src/hooks/useXxx.ts`に置く（現状フックは未使用だが、
  増える場合はこの配置にする）

## Git・コミット運用（提案。既存の慣習と違えば教えてください）

- 個人開発のため、通常の小さな修正は`main`への直接コミットでよい
- 「プロジェクト管理」機能のように複数ステップにまたがる大きな変更は、
  `feature/<内容を表す英語>`（例：`feature/project-management`）で作業ブランチを切り、
  動作確認後に`main`へマージする運用を推奨
- コミットメッセージは`feat:` `fix:` `docs:` `refactor:`等のプレフィックス＋日本語本文
  （これまでの`commit-message-*.md`の書き方を踏襲）。Claudeは実際に`git commit`は実行せず、
  メッセージ案をチャットまたは`commit-message-*.md`で提示するだけに留める

## `.claude/rules/` の構成（ファイル種別ごとの詳細ルール）

```
.claude/rules/
├── code-style.md       # src/**/*.{ts,tsx} 共通：ファイルヘッダーコメント等の品質ルール
├── ui-theming.md        # コンポーネント／ページ：配色・レスポンシブの規約
├── kanban-priority.md   # カンバン・ガントチャート：遅延最優先ルール等
├── types.md             # src/types/：型を崩さないための注意
├── supabase.md          # SQLマイグレーション・src/lib/：DB・RLSの規約
└── docs.md              # *.md：ドキュメントの役割分担
```

これらは対象パスのファイルを読み書きするときに自動で読み込まれるため、無関係な作業では
コンテキストに混ざらない。ルールを追加・修正する場合も、対応するファイルだけを編集すればよい。

## ドキュメント構成の方針（重複を避けるため）

- **CLAUDE.md（このファイル）**：セッションをまたいでも変わらない事実だけ。頻繁に変わる
  「今のタスク状況」は書かない
- **`引継ぎメモ.md`**：スレッドをまたぐ一時的な最新状況スナップショット。古くなったら
  都度上書きしてよい
- **`.claude/rules/`**：ファイル種別ごとの実装ルール。全体に関わる大原則は`規約.md`
  （本ファイル冒頭で読み込み済み）に残し、ここでは各領域固有の具体ルールのみを書く
- **`仕様書.md` / `認証・DB設計書.md`**：実務で使う設計書として専門的・簡潔に保つ。
  会話的な文言や合意の経緯は書かない
- **`学習ノート.md`**：初心者向け解説はすべてここに集約する。設計書側には書かない
