---
paths:
  - "src/types/**/*.ts"
---

# 型定義を変更するときの注意

- `Task` `TaskStatus` `TaskPriority`等はアプリ全体から参照される単一の型定義。
  ここを変更すると影響範囲が広いため、変更前に**利用箇所を必ずgrepしてから**編集する
- 型を緩めて（オプショナル化・`any`化）エラーを回避しない。呼び出し側の不整合を直す
- Supabaseのテーブル定義（`認証・DB設計書.md`）とフィールド名・NULL許容が一致しているか、
  変更のたびに確認する
