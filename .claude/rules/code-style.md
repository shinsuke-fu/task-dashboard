---
paths:
  - "src/**/*.ts"
  - "src/**/*.tsx"
---

# コード品質ルール（src全般）

- **ファイルヘッダーコメント**：新規にコンポーネント／主要ロジックファイルを作るときは、
  既存ファイル（例：`src/components/kanban/TaskCard.tsx`）にならい、ファイル先頭に
  以下の形式のコメントブロックを付ける。
  ```
  /**
   * src/xxx/Yyy.tsx
   * -----------------------------------------------------------------------
   * 【役割】
   *   （このファイルが何を担当するか）
   * 【主な処理】
   *   1. （処理の流れを箇条書きで）
   * -----------------------------------------------------------------------
   */
  ```
- **`any`を使わない**：型エラーは、Propsの渡し漏れやimportパスの不整合を疑って直す。
  `any`で握りつぶさない（`規約.md`②）
- **非自明なロジックには「なぜ」を書く**：単なる処理内容の説明ではなく、なぜその実装に
  なっているか（例：iOS Safariのタッチ操作でネイティブD&Dが効かないため、等）を
  コメントで残す。既存ファイルのコメントの粒度に合わせる
- **破壊的な操作の前に確認ダイアログを挟む**：タスク削除・退会など元に戻せない操作は、
  `window.confirm`等で誤操作防止を入れる（`TaskCard.tsx`の削除ボタンが例）
- **アイコンのみのボタンには`aria-label`を付ける**：テキストラベルがないボタン
  （閉じるボタン等）は`aria-label`でスクリーンリーダー対応する（`ImageLightbox.tsx`が例）
- **操作ボタンは権限を持つ人にしか出さない**：RLSで許可されている人（作成者・確認者等）
  以外には、操作ボタンの代わりに現在の状態を示す文言だけを出す（`TaskCard.tsx`が例）
