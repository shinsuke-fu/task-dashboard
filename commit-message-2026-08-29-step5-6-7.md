# コミットメッセージ案（2026-08-29・ステップ5＋6＋7＋ポーリング不具合修正）

前回のコミット（ステップ1+3+4＋追加分）以降の変更分です。今回も前回コミットが存在するため、
通常通り`git add`→`git commit`で問題ありません。

（このファイル自体はGitにコミットしない、チャット経由で渡すだけの補助ファイルです）

---

## コミットメッセージ

```
feat: プロジェクトのメンバー管理・オーナー譲渡と、タスクのプロジェクト間移動を追加

- 新規MemberManagementModal.tsxを追加し、プロジェクトオーナーがメンバーの追加・削除・
  オーナー譲渡を行えるようにした（transfer_project_ownership()関数をRPC経由で利用）
- プロジェクト管理タブに、オーナー向け「メンバー管理」ボタン・非オーナー向け「抜ける」
  ボタンを追加
- Task型にprojectIdを追加し、TaskForm.tsxの編集画面に「プロジェクト」欄を追加。
  自分が参加している他のプロジェクトへタスクを移動できるようにした
- 作業担当者・確認者の選択候補を、常に選択中プロジェクトのメンバーのみに絞り込むよう変更
  （プロジェクト機能導入前は登録ユーザー全員が対象だった）
- プロジェクトを変更すると、移動先のメンバーでなくなる担当者・確認者だけを
  自動的に選択から外す（全リセットではなく、該当者だけをクリア）
- 上記の絞り込みに使うprojectMembersを、ログイン時点で取得するよう変更
  （元は「プロジェクト管理」タブを開いたときだけ取得していた）

fix: プロジェクト切り替え後もタスク一覧の20秒ポーリングが古いプロジェクトを
     参照し続ける不具合を修正

- 通知ベル用のタスク再取得ポーリング（setInterval）が、依存配列に
  currentProjectIdを含めていなかったため、クロージャがタイマー作成時点の
  プロジェクトを握ったまま更新されず、プロジェクト切り替え後も最大20秒以内に
  別プロジェクトのタスクで一覧が上書きされてしまう不具合があった。
  依存配列にcurrentProjectIdを追加して解消

feat: 退会フローにオーナー引き継ぎのステップを組み込み

- 新規OwnershipHandoverSection.tsxを追加。設定＞データ画面で、自分がオーナーかつ
  他にもメンバーがいるプロジェクトが1件以上ある間は、通常の退会ボタンの代わりに、
  プロジェクトごと新オーナーを選んで譲渡できるセクションを表示する。譲渡が完了して
  対象が0件になると、自動的に通常の退会ボタンへ切り替わる
- delete_own_account()（Supabase側のsecurity definer関数）に、自分がオーナーで
  他に誰もメンバーがいないプロジェクトをタスクごと削除するステップを追加
- 退会確認ダイアログ・警告文言に、自分1人だけのオーナープロジェクトが
  削除される旨を追記
```

---

## 補足

- **Supabase側の変更あり**：`supabase-migration-account-deletion-owner-handover.sql`
  （`delete_own_account()`の更新分）は、**ユーザー側で既にSupabaseのSQL Editorで実行済み**
  です。このコミットにはSQLファイルも含めますが、DB側への追加作業は不要です。
- `git add` / `git commit` / `git push`はユーザー側で実行してください
  （`feature/project-management`ブランチ上）。
