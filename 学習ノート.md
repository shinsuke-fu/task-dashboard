# 学習ノート — WORK PLUSのコードで学ぶ技術解説

このドキュメントは`基本設計書.md`や`詳細設計書_認証DB編.md`とは目的が違います。あちらは
「このアプリが何を実装しているか」を記録する実務向けの設計書ですが、こちらは
「なぜこう書くのか」を、実際にこのプロジェクトで使われているコードを教材にしながら
理解するための学習用ノートです。設計書側の「実装後の補足」「発生した不具合と対処」
に類する経緯・トラブルシューティングの記録も、原則としてこちらに集約しています。

技術スタック（React 19 / TypeScript / Vite / Tailwind CSS v4 / Supabase）を、それぞれ
「そもそも何のためにあるのか」から順に説明していきます。細かい構文よりも、
「このアプリのどの場面で、何のために使われているか」を掴むことを優先しています。

作成日：2026-08-25
2026-09-02：9章を新設し、設計書側にあった不具合対応の経緯・学びをこちらへ移設

---

## 0. 全体像：このアプリはどう動いているか

まず技術要素の前に、大まかな全体像から掴んでおくと理解しやすくなります。

```
【ブラウザの中】
React（画面の見た目と操作） ── TypeScript（型チェック） ── Tailwind CSS（見た目のスタイル）
         │
         │ @supabase/supabase-js（Supabaseと通信するためのライブラリ）
         ▼
【Supabase（外部のクラウドサービス）】
Auth（ログイン・ユーザー管理） / PostgreSQL（データベース） / Storage（画像などのファイル保存）
```

このアプリには自分で書いた「サーバーのコード」（Node.js/Expressなど）が存在しません。
ブラウザ上のReactが、Supabaseという外部サービスに直接データの読み書きをお願いする構成に
なっています。これは「BaaS（Backend as a Service）」と呼ばれる構成で、認証やデータベースの
面倒な部分（パスワードの安全な管理、SQLサーバーの運用など）をSupabase側に任せてしまうことで、
フロントエンドの実装に集中できるのが利点です。

以降の章では、この図の中に出てくる要素を1つずつ見ていきます。

---

## 1. React の基本

### 1.1 コンポーネントとは何か

Reactでは、画面の部品を「コンポーネント」という関数の単位で作ります。関数が最終的に
「これを画面に表示してください」というJSX（HTMLのような書き方ができる記法）を返す、
というのが基本の形です。

```tsx
// イメージ（実際のTaskCard.tsxを単純化した例）
function TaskCard({ task }: { task: Task }) {
  return <div className="rounded-xl p-4">{task.title}</div>;
}
```

このアプリでは`Sidebar`、`TaskCard`、`KanbanBoard`、`SettingsView`など、画面の部品ごとに
コンポーネントが分かれています。`src/components/`以下のフォルダ構成が、そのままコンポーネントの
一覧になっています。

### 1.2 Props（親から子へのデータの受け渡し）

コンポーネントは、関数の「引数」のような形で親コンポーネントからデータを受け取ります。これを
Props（プロパティ）と呼びます。Propsは**読み取り専用**で、子コンポーネント側で勝手に書き換えては
いけません。

`Sidebar.tsx`の実際の例：

```tsx
interface SidebarProps {
  currentView: string;
  onViewChange: (view: string) => void;
  isOpen: boolean;
  onToggle: () => void;
  onLogout: () => void;
  onOpenSettings: () => void;
}

export default function Sidebar({ currentView, onViewChange, isOpen, /* ... */ }: SidebarProps) {
  // currentView や isOpen を使って表示を切り替えるだけで、
  // Sidebar自身がこれらの値を書き換えることはしない
}
```

`onViewChange`のように「関数そのもの」もPropsとして渡せるのがポイントです。子コンポーネントは
「この関数を呼んでください」という形で親に処理をお願いします（4章で詳しく説明します）。

### 1.3 State（`useState`）と再描画

コンポーネントが内部に持つ「変化する値」をState（ステート）と呼びます。`useState`というReactの
機能（フック）で作ります。Stateの値が変わると、Reactが自動的にそのコンポーネントを再描画します。

`src/hooks/useTheme.ts`の実際の例（テーマの状態管理。`App.tsx`から`useTheme()`として
呼び出すカスタムフックの中身です。8.8参照）：

```tsx
const [theme, setTheme] = useState<AppTheme>(() => {
  return (localStorage.getItem('dashboard_theme') as AppTheme) || 'graphite-dark';
});
```

- `theme`：現在の値（読み取り用）
- `setTheme`：値を変更するための関数
- `useState`に渡している`() => {...}`は「初期値を計算する関数」で、`localStorage`に保存済みの
  値があればそれを、無ければ`'graphite-dark'`（GRAPHITEテーマ）を初期値にしています

`setTheme('ocean-dark')`のように呼び出すと、`theme`の値が変わり、それを参照している
全てのコンポーネント（ヘッダーの配色など）が自動的に再描画されます。

### 1.4 Props で下に渡し、コールバック関数で上に伝える

これがこのアプリで最も大事な設計方針です。`規約.md`では「Single Source of Truth（単一の真実の
源泉）」と呼ばれています。

- **データは上から下へ**：`App.tsx`が持っているState（`tasks`など）を、Propsとして子コンポーネント
  に渡していきます
- **変更依頼は下から上へ**：子コンポーネントは自分でデータを書き換えず、Propsとして受け取った
  関数（`onProcessAction`など）を呼び出すことで、「こう変更してほしい」と親（最終的には
  `App.tsx`）にお願いします

```
App.tsx（tasksを持っている）
  │ tasks を Props で渡す
  ▼
TaskCard（tasks[0] を表示するだけ）
  │ ボタンが押されたら onProcessAction(id, 'approve') を呼ぶ
  ▲
App.tsx（実際に tasks を書き換えるのはここだけ）
```

なぜこうするのか：もし複数のコンポーネントがそれぞれ勝手にデータを書き換えられると、
「今どの値が正しいのか」を追うのが難しくなり、バグの温床になります。「データを持つ場所」と
「データを書き換える場所」を1箇所（`App.tsx`）に集約することで、この問題を防いでいます。

### 1.5 `useEffect`（副作用）

「画面の描画」そのものではなく、「値が変わったときに何か処理をしたい」という場合に使います。

`src/hooks/useTheme.ts`の実際の例（テーマが変わるたびに`localStorage`へ保存する）：

```tsx
useEffect(() => {
  localStorage.setItem('dashboard_theme', theme);
}, [theme]);
```

`[theme]`の部分を「依存配列」と呼びます。「`theme`が変わったときだけ、このカッコの中の処理を
実行してください」という意味です。Supabaseからのデータ取得（`profiles`テーブルの読み込みなど）も、
基本的にはこの`useEffect`の中で行っています。

### 1.6 `useMemo`（計算結果の使い回し）

「毎回計算し直すと無駄が多い処理」の結果を、依存する値が変わらない限り使い回す仕組みです。
このアプリでは、フィルター条件（担当者・カテゴリ・優先度）に応じてタスクを絞り込む
`filterTasks`の呼び出しなどで使われています。無駄な再計算を防ぎ、動作を軽くする役割です。

---

## 2. TypeScript の基本

TypeScriptは、JavaScriptに「型」という仕組みを追加した言語です。「この変数には文字列しか
入れられない」「この関数は数値を返す」といったルールをコード自体に書いておくことで、
実行する前（コーディング中やビルド時）に間違いを検出できるのが最大のメリットです。

### 2.1 `interface`（データの形を定義する）

`src/types/task.ts`の実際の例：

```ts
export interface Task {
  id: string;
  title: string;
  description?: string;
  status: TaskStatus;
  // ...
  createdBy: string;
}
```

`interface`は「このオブジェクトはこういうプロパティを持っています」という設計図です。
コード中で`Task`型として扱われている変数は、このプロパティを必ず（`?`が付いているものは
任意で）持っていることが保証されます。

- `description?: string`の`?`は「オプショナル」といい、そのプロパティが無くてもよい
  （`undefined`の可能性がある）ことを示します
- `createdBy: string`のように`?`が無いものは**必須**です。もし`createdBy`を渡し忘れると、
  TypeScriptがビルド時にエラーとして教えてくれます（実際、今回の作業中に発生した
  `npm run build`のエラーも、まさにこの「必須プロパティの渡し忘れ」を検出したものでした）

### 2.2 Union型（`|`）

「これか、これか、これ」という限られた選択肢だけを許可する型です。

```ts
export type TaskStatus = 'todo' | 'doing' | 'review' | 'done';
```

こう定義しておくと、`status`に`'todo'`や`'done'`以外の文字列（例えば typo で`'don'`など）を
入れようとした時点で、TypeScriptがエラーを出してくれます。このアプリの`AppTheme`型
（12種類のテーマ名）も同じ仕組みです。

### 2.3 `Record<K, V>`

「キーの型がK、値の型がV」というオブジェクトの型です。

```ts
const themeLabels: Record<AppTheme, string> = {
  'graphite-dark': 'GRAPHITE',
  'sage-dark': 'SAGE',
  // ... 12種類すべて
};
```

こう書くことで、「`AppTheme`の12種類すべてに対応するラベルが用意されているか」をTypeScriptが
チェックしてくれます。うっかり1つ書き忘れると、これもビルド時にエラーになります。

### 2.4 `Omit<T, K>`

既存の型から、一部のプロパティを除いた新しい型を作るための機能です。今回のビルドエラーの
原因と修正にも関わった型なので、実例で見てみます。

```ts
// 「Taskからidとstatusを除いた型」を、フォームからの入力として受け取りたい
onAddTask: (task: Omit<Task, 'id' | 'status' | 'createdBy'>) => void;
```

`id`（DBが自動採番）や`status`（新規作成時は常に`'todo'`）、`createdBy`
（`useTasks.ts`の`handleSaveTask`内でログイン中のユーザーIDから設定する）は、フォームの
入力項目ではないため、あえて型から除外して「フォームが渡すべき項目」だけに絞り込んでいます。

---

## 3. Vite

Viteは「開発用サーバー」と「本番用ビルド」を行うツールです。`npm run dev`で開発中のプレビューを
高速に立ち上げ、`npm run build`で本番公開用の静的ファイル（`dist/`フォルダ）を生成します。
React Create App等の旧世代のツールに比べて起動・反映が高速なのが特徴です。

`npm run build`は内部で2つのことを行っています。

```
tsc -b && vite build
```

1. `tsc -b`：TypeScriptの型チェック（型エラーがあればここで止まります。今回の
   `TaskForm.tsx`のエラーもここで検出されました）
2. `vite build`：型チェックを通過したら、実際にブラウザ用のファイルへ変換・最適化する

---

## 4. Tailwind CSS v4

### 4.1 ユーティリティクラス

Tailwindは、CSSを別ファイルに書く代わりに、`className`に小さな役割ごとのクラス名を
並べていくスタイルの手法です。

```tsx
<div className="w-14 h-14 rounded-full bg-base border border-border-card flex items-center justify-center">
```

- `w-14 h-14`：幅・高さ
- `rounded-full`：正円にする
- `bg-base` / `border-border-card`：後述するカスタムカラー変数を使った背景色・枠線色
- `flex items-center justify-center`：Flexboxで中央寄せ

`md:w-8`のように接頭辞を付けると「画面幅が`md`（中サイズ）以上のときだけ適用」という
レスポンシブ対応になります。

### 4.2 `@theme`とCSS変数によるテーマ管理

Tailwind CSS v4では、`@theme`ブロックでCSS変数を定義し、そこにテーマカラーを持たせられます。
このアプリの`src/index.css`には、12種類の配色テーマ（GRAPHITE・SAGE・OCEAN…）がそれぞれ
`[data-theme="graphite-dark"] { --theme-accent: #4dd4c0; ... }`のような形で定義されています。

```css
:root, [data-theme="graphite-dark"] {
  --theme-base: #101214;
  --theme-accent: #4dd4c0;
  /* ... */
}
```

HTML要素に`data-theme="ocean-dark"`のような属性を付け替えるだけで、これらのCSS変数の値が
まるごと切り替わり、`bg-base`や`text-accent`といったクラスを使っている全部品の色が
一括で変わる、という仕組みです。「配色パターンを増やす」＝「このブロックを1つ追加する」
だけで済むように設計されています。

---

## 5. Supabase

SupabaseはPostgreSQL（データベース）を中心に、認証（Auth）・ファイル保存（Storage）・
リアルタイム通信（Realtime）などをまとめて提供してくれるクラウドサービスです。

### 5.1 Auth（認証）

「メールアドレス＋パスワードでログインする」という仕組み自体を、自分で実装せずに使えます。

```ts
await supabase.auth.signInWithPassword({ email, password });
await supabase.auth.signOut();
```

ログイン状態（セッション）はSupabase側が管理しており、`onAuthStateChange`というイベントを
購読することで、「ログインした／ログアウトした」というタイミングをReact側で検知できます。

### 5.2 PostgreSQLとテーブル

Supabaseの実体はPostgreSQLというリレーショナルデータベースです。`tasks`や`profiles`といった
「テーブル」（表）にデータが保存されます。フロントエンドからは、SQLを直接書く代わりに
`@supabase/supabase-js`というライブラリ経由でデータを操作します。

```ts
const { data, error } = await supabase
  .from('profiles')
  .select('id, display_name, avatar_url');
```

### 5.3 RLS（Row Level Security＝行レベルセキュリティ）

「このユーザーはこのテーブルのどの行を見て良い／編集して良いか」というルールを、
アプリのコードではなく**データベース自体**に書き込める仕組みです。

例えば`tasks`テーブルには「削除できるのは作成者（`created_by`）本人だけ」というルールを
設定しています。これにより、仮にブラウザの開発者ツールから直接Supabaseへ削除リクエストを
送りつけても、DB側が本人以外のリクエストを拒否してくれます。「フロントエンドのコードで
ボタンを隠す」だけでは防げない、より確実な制御です。RLSの実際の落とし穴は9.1・9.2参照。

### 5.4 `security definer`関数（退会機能の例）

SupabaseのAnon Key（公開鍵）では、`auth.users`テーブル（ログイン情報そのもの）を直接
削除することはできません。自分自身のアカウントであってもです。そこで、「特定の処理だけを、
関数の定義者の権限で実行してよい」という`security definer`という仕組みのPostgres関数を使います。

```sql
create or replace function public.delete_own_account()
returns void
language plpgsql
security definer
set search_path = public
as $$
begin
  delete from public.tasks where created_by = auth.uid();
  delete from auth.users where id = auth.uid();
end;
$$;
```

`auth.uid()`は「今この関数を呼んでいる本人のユーザーID」を返す関数です。これにより、
「本人のデータだけを、本人の意思で削除する」という安全な操作を、フロントエンドからの
1回の呼び出し（`supabase.rpc('delete_own_account')`）で実現しています。

### 5.5 Storage（ファイル保存）

画像などのバイナリファイルは、テーブルではなく「バケット」という入れ物に保存します。
このアプリのアバター画像は`avatars`という公開バケットに、`{ユーザーID}/avatar`という
パスで保存されます。RLSと同じ考え方で、「自分のフォルダにしかアップロードできない」
というポリシーをバケット側にも設定しています。

---

## 6. このアプリならではの設計パターン

最後に、React/TypeScript/Supabaseの一般論ではなく、このアプリを作る過程で採用された
設計上の工夫をまとめます（詳しくは`基本設計書.md`・`規約.md`を参照）。

- **Single Source of Truth**：状態の所有権は`App.tsx`（正確には`App()`コンポーネント
  インスタンス1つ）に一元化されており、他のコンポーネントはProps経由でしか触れない
  （1.4参照）。`useState`の呼び出し自体は`src/hooks/`配下のカスタムフックへドメインごとに
  分割されているが、所有権の一元化という原則自体は変わらない（8.8参照）
- **一面集約型SPA**：React Routerなどの外部ライブラリを使わず、`currentView`という
  1つの文字列のStateだけで画面切り替えを行う、最も単純なルーティング方式
- **遅延最優先ルール**：期日超過のタスクは、他のどんな状態（進行中／査読待ちなど）よりも
  「遅延」の見た目を優先して表示する、UI上の一貫したルール
- **共通関数への集約**：「今日の日付」の計算（`src/utils/date.ts`）や、タスクの絞り込み
  （`src/utils/assignee.ts`の`filterTasks`）のように、複数の画面で必要になる処理は
  1箇所の関数にまとめ、コピペで似たコードが増えるのを防いでいる

---

## 7. テスト（Vitest）とCI（GitHub Actions）とは

2026-08-31に、このアプリに初めてテストとCIを導入しました。「テストって何をしているのか」
「毎回自分でテストを書かないといけないのか」「CIバッジって何なのか」を、実際に起きた
出来事も交えながら説明します。

### 7.1 そもそも「テスト」とは何か

普段の開発では、コードを直したら自分でアプリを操作して「ちゃんと動くか」を目で確認します。
テストは、その確認作業の一部を**コードとして書いておき、コマンド一発で再現できるようにする**
ものです。

```ts
// 「2026-08-29が今日のとき、2026-08-31までの残り日数は2であるべき」というテスト
it('未来の日付なら正の日数', () => {
  expect(getDaysDiffFromToday('2026-09-03')).toBe(5);
});
```

`expect(実際の値).toBe(期待する値)`という形で「こうなっているはず」を書いておき、
`npm run test`を実行すると、書いたテストが全部その通りになっているかを自動でチェックしてくれます。

### 7.2 全部のコードにテストを書く必要はない

このアプリでは、`src/utils/date.test.ts`・`src/utils/assignee.test.ts`・
`src/components/kanban/TaskCard.test.tsx`の3ファイルしかテストがありません。他の
コンポーネント（`KanbanBoard.tsx`や`Sidebar.tsx`等）にはテストがありませんが、これは
書き忘れではなく意図的です。

テストを書く価値が高いのは、次のような部分です。

- **バグに気づきにくい部分**：`date.ts`の日付計算はタイムゾーンが絡み、目で見ただけでは
  正しいかどうか分かりにくい
- **壊れると実害が大きい部分**：`TaskCard.tsx`の「遅延最優先ルール」（規約③）は、この
  アプリの一番の売りなので、うっかり壊れると困る
- **見た目には現れにくい条件分岐**：「担当者・カテゴリ・優先度を組み合わせた絞り込み」
  （`filterTasks`）のような、パターン数が多い処理

逆に、単純に画面を組み立てているだけのコンポーネント（`DashboardView.tsx`等）は、
テストを書くコストの割に得られる安心感が小さいため、今回は対象外にしています。**今後
新しい機能を作るたびに、必ずテストを書かないといけないわけではありません**。「これは
壊れたら気づきにくい／困る」と思った処理にだけ足していく、くらいの温度感で大丈夫です。

### 7.3 テストの中身を分解してみる

`date.test.ts`を例に、テストファイルの構造を見てみます。

```ts
describe('getDaysDiffFromToday', () => {        // ①「何についてのテストか」のグループ
  beforeEach(() => {                              // ②各テストの前に必ず実行する準備
    vi.useFakeTimers();                           //   「今日」を固定する（毎回日付が
    vi.setSystemTime(new Date('2026-08-29T03:00:00Z')); //   変わると結果も変わってしまうため）
  });

  it('今日の日付を渡すと0になる', () => {          // ③個々のテスト（「〜のはず」を1つずつ）
    expect(getDaysDiffFromToday('2026-08-29')).toBe(0); // ④実際の値と期待する値を比較
  });
});
```

`TaskCard.test.tsx`は、ロジックだけでなく**画面の見た目**をテストしている点が少し違います。
`@testing-library/react`という道具を使い、実際にコンポーネントを描画（`render`）し、
画面に表示されている文字やCSSクラスを確認（`screen.getByText(...)`など）します。

### 7.4 実際に踏んだ落とし穴（生きた教材）

このテストを導入した直後、2つのバグに遭遇しました。実際のエラーメッセージと合わせて
振り返っておくと、次に似た問題に出会ったときのヒントになります。

1. **`ReferenceError: React is not defined`**：JSX（`<TaskCard ... />`のような書き方）を
   使うファイルなのに、`import React from 'react';`を書き忘れていたのが原因でした
2. **前のテストの結果が次のテストに残ってしまう**：`TaskCard.test.tsx`の2つ目のテストが、
   自分では「期日間近」のカードしか描画していないのに、なぜか1つ目のテストで描画した
   「遅延」バッジを見つけてしまう、という不思議な失敗がありました。原因は、Testing Library
   は本来テストごとに「前回描画した内容をきれいに片付ける」仕組み（cleanup）を持つのですが、
   その仕組みが自動で有効になる設定（`globals: true`）をこのプロジェクトでは使っていなかった
   ため、片付けが行われていなかったことでした。`src/test/setup.ts`に
   `afterEach(() => cleanup())`を明示的に1行足して解決しています

どちらも「コードのロジック自体は合っているのに、テストの設定・書き方が原因で失敗する」
というパターンで、テストを書き始めたころによくあるつまずきです。

### 7.5 CI（GitHub Actions）とは

CI（Continuous Integration）は、「pushするたびに、決まったチェックを自動で実行してくれる
仕組み」です。このアプリでは`.github/workflows/ci.yml`に設定を書いており、GitHubへ
pushするかPull Requestを作るたびに、GitHub側のサーバー上で自動的に次の3つを順番に
実行します。

1. `npm run lint`（コードの書き方チェック）
2. `npm run test`（さっき説明したテスト）
3. `npm run build`（型チェック＋本番用ビルド）

自分のPCで確認し忘れたまま間違ったコードをpushしてしまっても、CIが代わりに気づいて
教えてくれる、という保険のようなものです。README.md冒頭の「CI」バッジは、直近の
実行結果（緑＝成功／赤＝失敗）を表示しており、クリックすると実行ログも見られます。
このAIアシスタントの作業環境では`npm run build`が実行できないため、pushした後にCIが
チェックしてくれることで、その穴をある程度埋められます。

---

## 8. 開発の中で実際に起きたこと：不具合と学び

設計書（`詳細設計書_認証DB編.md`）は「今の設計がどうなっているか」を専門的・簡潔に記録する
場所と位置づけているため、「何にハマって、どう気づいて、どう直したか」という経緯・
トラブルシューティングの記録はこちらにまとめています。同じ轍を踏まないための教材です。

### 8.1 RLSの落とし穴①：再帰的RLSポリシー

プロジェクト管理機能の実装時、`projects` / `project_members`のRLSポリシーを、素朴に
「`project_members`テーブルを`EXISTS`句で自己参照する」形で書いたところ、Postgresの
「recursive RLS policy」エラー、あるいは意図しない全件非表示（既存タスクが画面から
見えなくなる）という形で症状が出ました。

原因は、`project_members`自身のSELECTポリシーを評価する際に、そのポリシー条件がまた
`project_members`を参照するという循環が発生していたことです。

**対処**：判定ロジックを`security definer`のヘルパー関数（`is_project_member` /
`is_project_owner`）に切り出しました。`security definer`関数の内部クエリはRLSの対象外
（関数定義者の権限で実行される）のため、自己参照によるループが起きません。**同じテーブルを
素朴な`EXISTS`で自己参照するRLSポリシーを書く場合は、必ずこのパターンでヘルパー関数を
経由させること**を教訓として設計書に残しています（詳細設計書_認証DB編.md3.2）。

### 8.2 RLSの落とし穴②：「鶏と卵」問題

プロジェクト新規作成が「新しい行が行レベルセキュリティポリシーに違反しています」
（RLS違反、`42501`）で失敗する不具合が発生しました。原因はINSERT自体ではなく、
`.insert().select().single()`のRETURNINGが、作成直後に走る`handle_new_project`トリガー
（作成者をオーナーとして`project_members`へ登録する処理）の完了を待たずに、SELECT用RLS
ポリシーの可視性チェックを受けてしまい、「作成した本人なのに、まだメンバー登録が完了して
いないので見えない」と判定される、という一種の「鶏と卵」問題でした。

**対処**：SELECTポリシーに「`auth.uid() = created_by`」も許可条件として追加しました。
**同じ組み合わせ（INSERT直後にAFTER INSERTトリガーが別テーブルへ権限系の行を追加し、かつ
RETURNINGでその場で結果を返す）のRLSポリシーを書くときは、この鶏と卵問題を疑うこと**が
教訓です。

### 8.3 `task_assignees` / `task_subtasks`のRLS更新漏れ（2026-09-02発覚）

プロジェクト管理機能導入時に`tasks`本体のRLSポリシーは「プロジェクトメンバーのみ」方式へ
更新しましたが、担当者中間テーブル`task_assignees`とサブタスクテーブル`task_subtasks`は
`project_id`列を持たない（`task_id`経由でしか所属プロジェクトを辿れない）ためポリシー変更の
対象から漏れており、初期のポリシー（ログイン済みなら誰でも閲覧・追加・削除可）のまま
残っていました。設計書側には「更新済み」と誤って記載してしまっていたため、2026-09-02の
ドキュメント監査（実コードとの突き合わせ）で発覚しました。

**実害**：`tasks`本体はプロジェクトメンバー以外には見えないため実際のタスク内容までは
読めませんが、中間テーブル自体はプロジェクトに無関係な認証済みユーザーであっても
全プロジェクト分を横断的に取得でき、`task_id`さえ分かれば追加・削除も通ってしまう状態
でした。`task_subtasks.title`には実際のサブタスク文言が入るため、ゲストのデモデータに
限らず一般ユーザーの内容も対象になり得ました。

**対処**：`task_id`から所属プロジェクトを辿るヘルパー関数`is_task_project_member`を新設し、
`tasks`と同じ方式に揃えました。

**教訓（重要）**：中間テーブル・子テーブルに`project_id`列を持たせず親テーブル経由で
プロジェクトを判定する設計にした場合、**親テーブルのRLSポリシーを変更したら、その親を
参照する全ての中間テーブル・子テーブルのRLSポリシーも同時に見直すこと**。本件は実装だけで
なくドキュメント側にも「更新済み」という誤記載が残ってしまっていたため、新しい参照テーブルを
追加した際は関連テーブル一覧を洗い出してから着手するのが望ましい、という教訓も残りました。

### 8.4 空文字列とnullの違いでDB挿入エラーになった話

Supabase移行直後、タスクを新規作成すると`400`エラーになる不具合がありました。原因は、
確認者（レビュアー）の候補が「担当者に選ばれていないユーザー」から絞り込む仕様のため、
ユーザーが自分1人しかいない段階では候補が0人になり、`TaskForm.tsx`側の`reviewerId`が
空文字列（`''`）のまま送信されていたことです。`tasks.reviewer_id`はuuid型の列のため、
空文字列を送るとPostgres側で`invalid input syntax for type uuid`エラーとなり、PostgREST
経由では`400`として現れていました。

**対処**：`handleSaveTask`内で、`taskData.reviewerId ?? null`（`??`は
`undefined`/`null`しかnullに変換しない）を`taskData.reviewerId || null`（空文字列も含めて
nullに変換する）に修正しました。「フォームの未選択状態が空文字列で表現されるUIコンポーネント
から、null許容のuuid列へ値を渡すときは、`??`ではなく`||`で空文字列もケアする必要がないか
確認する」という、地味だが再発しやすい教訓です。

### 8.5 ゲストログイン機能：デモデータ自動投入のレースコンディション

ゲストとしてログインした直後にデモ用プロジェクト・サンプルタスクを自動投入する処理
（`seedGuestDemoData`）を、当初は`src/pages/Login.tsx`側で`signInAnonymously()`の直後に
呼んでいました。しかしこれだと、ログインが成功した瞬間に`App.tsx`側のログイン検知
（`onAuthStateChange`）が走り、「プロジェクト一覧の取得（`refreshProjects`）」と
「デモデータの作成」が競合し、**取得の方が先に終わってしまい、新規プロジェクトが画面に
反映されない**という再現性の低い不具合がありました（毎回必ず発生するわけではなく、通信の
タイミング次第だったため発見が遅れました）。

**対処**：デモデータ投入の責務を`App.tsx`側へ移しました。`projectsLoaded`という「ログイン
直後の初回プロジェクト一覧取得が完了したか」を示すstateを新設し、これが`true`になって
（＝プロジェクトが本当に0件だと確定して）から初めてデモデータを作成するようにしました
（二重実行防止に`guestSeedStartedRef`を使用）。`Login.tsx`側の役割は`signInAnonymously()`を
呼ぶだけに戻しました。

**同じ「ログイン検知をトリガーに複数の非同期処理を走らせる」設計をする際は、それぞれの処理が
互いの完了を待たずに独立して走ることを前提に、明示的な完了フラグ（本件の`projectsLoaded`）で
順序を保証すること**が教訓です。

### 8.6 退会×ログアウトで発覚した`user_not_found`エラー

退会（`delete_own_account()`）で`auth.users`の自分自身の行を削除した直後に
`supabase.auth.signOut()`を呼ぶと、Supabase側が「JWTの`sub`クレームに対応するユーザーが
見つからない」（`code: user_not_found`。ブラウザの開発者ツールには
`POST /auth/v1/logout?scope=local 403 (Forbidden)`というネットワークエラーとしても表示される）
というエラーを返すことがあります（Supabase Auth側の既知の挙動）。ローカルのセッションを
クリアするという目的自体は達成できるため実害はなく、動作確認でも「ログイン画面へ正しく
戻る」「データが実際に削除される」ことを確認できています。

**対処**：`scope: 'local'`を指定した上でエラーは無視する共通ヘルパー
（`signOutAfterAccountDeletion`）を用意し、退会・ゲストログアウトの両方から呼ぶように
しました。ブラウザが自動的に表示するネットワークエラーログ自体は、アプリのコード側では
消せません。

このバグは、ゲストログイン機能で新たに生まれたものではなく、既存の退会機能
（`handleDeleteAccount`）にも元々あったものでした。ただし退会の実際のE2Eテストが保留中
だったため、これまで一度も実際に踏んでいなかった経路でした。ゲストログアウトが「自分自身を
削除してからサインアウトする」という同じ経路を初めて実際に通したことで発覚しました
（「テストしていない経路には、既知のバグが潜んでいても気づけない」という当たり前だが
実感しやすい教訓でもあります）。

### 8.7 その他の小さなつまずき

- **`JWT issued at future`（PGRST303）エラー**：開発中、ごく短期間だけ
  `{code: 'PGRST303', message: 'JWT issued at future'}`によりプロジェクト作成・タスク作成が
  401で失敗する現象が発生しました。PostgREST側が「このJWTの発行時刻が未来になっている」と
  判断して拒否するエラーで、Supabase内部のAuthとPostgREST間の時刻同期のズレ（30秒以上）が
  原因とされています。アプリのコードが原因ではなく、こちら側で直せる問題ではありません。
  時間を置く、またはクライアント端末のシステム時計を同期し直すことで解消することが多いようです
  （今回は自然に解消しました）
- **Vite 8とVitestのバージョン不整合**：`npm run build`実行時、`vitest.config.ts`で
  `TS2769: No overload matches this call`（`react()`プラグインの型が合わない）エラーが
  発生しました。原因は、プロジェクト本体の`vite`（8系・Rolldownベース）と、`vitest`
  （当初3系を指定）が、ピア依存として個別にダウンロードしていた別バージョンの`vite`とで
  `Plugin`型の形が食い違っていたことでした。`vitest`をVite 8に対応した4.1系に上げ、
  プロジェクト本体の`vite`をそのまま再利用するようにして解消しました

### 8.8 「Godコンポーネント」をカスタムフックへ分割する（2026-09-03）

`App.tsx`は開発が進むにつれて1713行まで肥大化していました。`useState`31個、`handle*`
ハンドラー31個、`useEffect`11個をすべて1つのコンポーネント関数の中に抱えている状態で、
これは「Godコンポーネント」と呼ばれるアンチパターンです。1つのファイルに関心事が
混ざりすぎると、どの状態がどの機能に関係しているのか把握しづらくなり、変更の影響範囲も
読みにくくなります。

**SSOTを崩さずに分割できるか？** 規約①「単一真実の源泉（SSOT）」は「状態は`App.tsx`で
一元管理する」と定めています。ここで大事なのは、この規約が禁止しているのは**子・孫
コンポーネントが状態を勝手に持ったり書き換えたりすること**であって、「コードが物理的に
1ファイルに収まっていること」までは要求していない、という点です。カスタムフック
（`useXxx`という名前の、Reactの機能を内部で使う普通の関数）は、呼び出し元の
コンポーネントインスタンスに紐づいて状態を持つので、`App()`という1つのコンポーネント
関数の中から9個のフックを呼び出しても、「状態を所有しているのはApp単体である」という
事実は変わりません。この整理により、`src/hooks/`配下へ①認証②担当者③テーマ④画面遷移
⑤プロジェクト⑥メンバー管理⑦タスク⑧通知⑨フィルターの9フックへ、1つずつ
（切り出す→`npm run build`で型チェック→動作確認、を繰り返す）段階的に分割し、
`App.tsx`は794行まで縮小しました。

**ハマりどころ①：TypeScriptのTDZ（Temporal Dead Zone）**。あるフックが、まだ切り出して
いない別の値（または別のフックの戻り値）を**直接引数として**必要とする場合、
`App.tsx`内でのフック呼び出しの順序を、その値の宣言より**後**に置く必要があります。

```ts
// NG：refreshProjectSummariesの宣言より前でuseProjects()を呼んでいるとTDZエラーになる
const { projects } = useProjects(..., refreshProjectSummaries); // ← まだ未定義
const { refreshProjectSummaries } = useProjectMembers(users);

// OK：宣言順を入れ替える
const { refreshProjectSummaries } = useProjectMembers(users);
const { projects } = useProjects(..., refreshProjectSummaries);
```

ポイントは、これが「直接引数として渡す」場合にだけ起きるということです。`useEffect`の
コールバックのような**クロージャ経由**の参照は、実際に呼ばれるのがレンダー後（宣言がすべて
終わった後）なので、宣言順を気にする必要がありません。

**ハマりどころ②：フック同士の循環依存**。`useProjects()`は`useProjectMembers()`の
`refreshProjectSummaries`を必要とするため、`useProjectMembers()`を先に呼ぶ必要があります。
では逆に`useProjectMembers()`が`useProjects()`の`projects`を必要としたら？　両方を
同時に満たす呼び出し順序は存在しません（循環依存）。この場合の解決策は、「無理にどちらかの
フックへ寄せようとしない」ことです。複数のフックの状態を組み合わせて計算するだけの派生値・
ハンドラー（例：`projectsNeedingOwnershipHandover`は`projects`と`projectMembers`の
両方が必要）は、`App.tsx`側に残しておけば、SSOTの観点（状態の所有権は一元化されたまま）
でも問題ありません。

**ハマりどころ③：`noUnusedLocals`（TS6133）は`npm run dev`では検出されない**。フックの
戻り値を`App.tsx`側で分割代入したものの、実際にはApp.tsx内のどこからも参照していない
プロパティが残っていると、`npm run build`（`tsc -b`）で初めて`'setCurrentView' is declared
but its value is never read`のようなエラーになります。`npm run dev`はこのチェックを
行わないため、ローカルでの動作確認だけでは気づけません。対策として、フックを切り出す
たびに「戻り値の各プロパティが本当に`App.tsx`内で使われているか」をgrepで機械的に
確認してから納品する、という運用に変えました。逆に、複数箇所で生のsetter関数がそのまま
呼ばれ続けているプロパティ（例：`setIsNotifOpen`）は律儀にラッパー関数を作らず、そのまま
返してよい、という判断もセットで必要です。

---

## 9. 次のステップ（学習の進め方の一案）

技術スタックへの理解を深めたい場合、以下のような順番で読み進めるのがおすすめです。

1. まず`src/types/task.ts`を開き、この学習ノートの2章と照らし合わせながら型定義を読む
2. 次に`App.tsx`冒頭のフック呼び出しの並び（`useAuthSession()` `useUsers()` …）を眺め、
   「このアプリがどんな状態をどのフックが持っているか」を把握する（`基本設計書.md`4章・
   4.0の表と対応しています）。個々のフックの中身は`src/hooks/`配下の各ファイルを開いて読む
3. `TaskCard.tsx`のような小さいコンポーネントを1つ選び、Propsとして何を受け取り、
   どのコールバック関数を呼んでいるかを追ってみる
4. 慣れてきたら`src/hooks/useTasks.ts`の中で、Supabaseの`select` / `insert` / `update`を
   呼んでいる箇所を探し、5章の内容と照らし合わせる
5. 仕組みが分かってきたら、8章の「実際に起きたこと」を読み、なぜその設計・ガードが
   必要だったのかを、起きた不具合と結びつけて理解する

一度にすべてを理解しようとせず、「今日はPropsとStateだけ」「次はSupabaseのSELECTだけ」の
ように区切って読むと負担が少なくおすすめです。
