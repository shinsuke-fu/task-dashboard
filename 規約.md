# 🚨 【 WORK PLUS 】AI共同開発・デグレ絶対遮断規約

本ドキュメントは、Vite + React + TypeScript + Tailwind CSS v4.0 で構築された、一面集約型タスク管理ダッシュボード「WORK PLUS」の品質と動作を維持するための【厳守規約】である。
AIアシスタントは、コード提案・リファクタリングを行う前に必ず本規約を全行精読し、100%遵守しなければならない。

---

## 1. 開発の基本設計思想（厳守ルール）

### ① 単一真実の源泉（Single Source of Truth）の徹底
* すべての大元データ（`tasks`）、フィルター状態、認証状態は親である `App.tsx` で一元管理する。
* 子・孫コンポーネントが破壊的に状態を書き換えるコードを一切禁止する。状態変更は必ず Props 経由で親の関数（`onUpdateStatus` 等）に直撃させること。
* ※2026-09-03：`useState`自体は`src/hooks/`配下のカスタムフックへドメインごとに切り出し済みだが、いずれも`App()`コンポーネントインスタンス1つだけが呼び出しており「所有権の一元化」自体は崩れていない。この規約が禁止しているのは子・孫コンポーネント側での状態所有・破壊的書き換えであり、親側でのカスタムフックへの物理的な分割は違反しない（詳細は`docs/基本設計書.md`4章）。

### ② 一面集約型SPA（シングルページアプリケーション）の維持
* ReactのStateによるインライン出し分け（`currentView` の文字判定）による最軽量ルーティングを死守すること。
* 外部ルーティングライブラリ（React Router等）を勝手に導入したり、画面遷移によって状態が消失する設計に変更してはならない。

### ③ 遅延最優先ルール（Overdue Priority）の絶対死守
* 完了（`done`）以外のすべてのタスクにおいて、期日（`endDate`）が基準日（今日）より過去の場合は、ステータス（進行中、承認待ち等）に関わらず、一律で「遅延（ローズ色枠・パルスバッジ等）」のデザインを最優先で適用すること。他の状態のUIで遅延表示を上書きしてはならない。

---

## 2. コード記述・リファクタリングの禁止事項（バグ撲滅）

### ① ハリボテ・ダミーコードへの勝手な書き換え禁止
* 実装済みの「その場で理由を自由に入力できる差し戻しモーダル（`RejectReasonModal`）」や「確認者（レビュアー）の個別選択ロジック」を、固定文字やダミーコードに先祖返り（デグレ）させてはならない。

### ② TypeScript 型定義の順守
* `src/types/task.ts` に定義されている厳格な型（`Task`, `TaskStatus` 等）を絶対に崩さないこと。
* 型エラーが発生した場合は、Propsの渡し漏れやインポートパスの不整合を精査し、安易に `any` 型を使用して型安全性を放棄してはならない。

### ③ Tailwind CSS v4.0 への最適化とCSS変数の尊重
* デザインはすべて `src/index.css` の `@theme` に定義されたカスタムカラー変数（`bg-base`, `bg-card`, `text-accent`, `border-border-card` 等）を100%使用すること。
* 既存のマットダークな世界観、極細SVGアイコン（`stroke-[1.2]` 〜 `stroke-2`）の美しさを崩す派手な装飾や絵文字の追加を一切禁止する。

---

## 3. レスポンス・出力ルール（Claude専用仕様）

### ① トークン制限に配慮した「ピンポイント差分出力」
* 1つのファイル全体のコードを出力すると、長文制限によってコードが途切れる原因となる。
* 修正やコード提示を行う際は、必ず**「変更するファイル名」「挿入・書き換えの具体的な場所（どの関数のすぐ下か等）」を明確に指定し、差分コードのみを最軽量で提示**すること。

### ② 「引き算の美学」の尊重
* コードを増やす提案よりも、不要なプロパティ（例: `Sidebar` の使われていない `theme` プロパティなど）を削る「綺麗な引き算」を最優先すること。
* 新たな外部ライブラリの導入を提案する場合は、ブラウザ標準APIやTailwindの標準機能で代替できないかを極限まで検証すること。

---

上記の規約を破るコード提案はすべて「差し戻し」の対象とする。
理解したら、現在のファイル構成と本規約の防衛を念頭に置き、「WORK PLUS開発プロジェクトを承諾しました。指示をどうぞ」と簡潔に応答せよ。
