# WORK PLUS（task-dashboard）仕様書

このドキュメントは、現時点のコードを読み解いてまとめた「現状仕様書」です。
実装済みの機能・画面構成・データモデルを、実務の仕様書と同じ体裁で記録することを目的としています。

React／TypeScript／Supabaseといった技術要素そのものの解説（初心者向け）は、このドキュメントでは
扱いません。`学習ノート.md`に、このアプリのコードを教材にした技術解説をまとめてあります。

作成日：2026-08-18（この時点のソースコードをもとに作成）

### 更新履歴

- 2026-08-20：担当者の複数選択UI、差し戻しモーダルのバリデーション修正、日付デフォルトの修正、
  残り日数計算の堅牢化、通知ベルの実装を反映
- 2026-08-22：スケジュール画面（月間カレンダー＋祝日API）、サブタスク（チェックリスト）機能、
  スマホレイアウトの全体対応、設定ページ（テーマ切替・通知ON/OFF・サンプルデータリセット）の
  新設を反映
- 2026-08-23：Supabase（PostgreSQL + Auth + RLS）によるバックエンド実装を反映。ログイン・
  ユーザー管理・タスクの保存先が、ブラウザの`localStorage`（自分のブラウザ内だけ）から実際の
  データベース（複数ユーザー間で共有）に切り替わった。配色テーマ・通知ON/OFF設定は個人のUI設定
  として引き続き`localStorage`に保存
- 2026-08-25：配色テーマを12種類（ダーク6種／ライト6種、既定はGRAPHITE）に再設計。設定ページを
  タブ構成（プロフィール／テーマ／通知／データ／アプリについて）へ拡張し、表示名編集・アバター
  画像アップロード・パスワード変更・退会（アカウント削除）・アプリ紹介（アプリについて）の
  各機能を追加
- 2026-08-25（続き）：アプリについてタブのロゴをSidebar/Loginと同じ角丸四角（`rounded-xl`）・
  黒文字固定に統一（ロゴは装飾要素のため黒固定のままでよいという判断）。アバター画像クリックで
  原寸表示できる`ImageLightbox`を追加（ヘッダー・プロフィールタブ共通）。アクセントカラーを
  背景に使うボタン・バッジ（新規作成ボタン・タイムラインの締切ラベル等）の文字色は、
  `--theme-on-accent`変数でダーク系＝黒／ライト系＝白に統一し、ライト系テーマでの可読性低下
  （黒文字固定によるコントラスト不足）を解消
- 2026-08-27：タブレット幅でサイドバーを開いた際にタスクボード・スケジュール画面のカラムが
  極端に狭くなり崩れる不具合を修正（6.5・6.6・6.10参照）。カンバンの「確認者の承認待ち」表示を
  「承認待ち」に簡略化
- 2026-08-31：未使用だった`App.css`・`components/SettingsModal.tsx`を削除。`Vitest` +
  `Testing Library`によるテストを導入し、日付計算・タスク絞り込み・遅延最優先ルール（規約③）に
  最低限のユニットテストを追加。GitHub Actionsで`lint`・`test`・`build`を自動実行するCIを追加。
  READMEを現在の実装内容に合わせて全面的に書き直し（7.1・7.2・8章参照）
- 2026-08-31（続き）：テスト実行時の`ReferenceError: React is not defined`（テストファイルの
  JSX変換の問題）と、各テスト後のDOM cleanup漏れ（前のテストの描画結果が残ってしまう問題）を
  修正。テスト専用の`tsconfig.test.json`を追加し、本番ビルドの型チェックからは独立させたまま
  エディタ上でjest-domの拡張マッチャーが正しく型解決されるようにした
- 2026-08-31（続き）：`npm run build`実行時、`vitest.config.ts`で
  `TS2769: No overload matches this call`（`react()`プラグインの型が合わない）エラーが発生。
  原因は、プロジェクト本体の`vite`（8系・Rolldownベース）と、`vitest`（当初3系を指定）が
  ピア依存として個別にダウンロードしていた別バージョンの`vite`とで、`Plugin`型の形が
  食い違っていたこと（Vite 8対応前のVitestは、プロジェクトの`vite`を再利用せず
  `node_modules/vitest/node_modules/vite`という別のvite一式を持ってしまう）。
  `vitest`をVite 8に対応した4.1系に上げ、プロジェクト本体の`vite`をそのまま再利用する
  ようにして解消した
- 2026-08-31（続き）：**ゲスト（匿名）ログイン機能を追加**。ポートフォリオサイト経由の
  訪問者が、会員登録・パスワード入力なしでその場でアプリを試せるようにするための機能
  （`supabase-migration-guest-login.sql`。詳細は`認証・DB設計書.md`12章参照）。
  - ログイン画面（`pages/Login.tsx`）に「ゲストとしてログイン」ボタンを追加。Supabaseの
    匿名認証（`signInAnonymously`）でその場でログイン状態になれる
  - ログイン直後、`App.tsx`側でデモ用プロジェクト＋サンプルタスク5件（todo/doing/review/done
    の各ステータス・優先度・カテゴリをばらけさせ、1件はあえて期日超過にしてある）を自動投入し、
    最初から動いている状態で見てもらえるようにした（`seedGuestDemoData`）
  - ログアウト時、ゲスト（匿名）アカウントの場合は既存の退会機能（`delete_own_account`）を
    呼んでからサインアウトすることで、デモデータ・匿名アカウント自体がDBに溜まり続けない
    ようにしている（6.1・6.9参照）
- 2026-08-29：**「プロジェクト管理」機能を実装**（`プロジェクト管理機能_要件定義書.md`参照。
  従来「近日公開」のプレースホルダーだったサイドバーの項目が実際に機能するようになった）。
  複数プロジェクトを作成し、プロジェクトごとにタスク・メンバーを分けて管理できる。
  主な変更点：
  - `projects` / `project_members`テーブルを新設し、`tasks`に`project_id`（NOT NULL）を追加。
    既存タスクは「（移行前タスク）」という既定プロジェクトへ自動移行した
  - サイドバーの「プロジェクト管理」がアコーディオン化し、素早い切り替え・新規作成の導線になった
    （メニュー内の並び順は最下部に変更）。「すべて管理→」から一覧・作成・編集・削除・
    検索/ステータスタブ絞り込みができる専用タブ（`ProjectManagementView.tsx`）を新設
  - プロジェクトのオーナーはメンバーの追加・削除・オーナー譲渡ができる
    （`MemberManagementModal.tsx`）。オーナー以外は「抜ける」でプロジェクトから脱退できる
  - タスク編集画面（`TaskForm.tsx`）に「プロジェクト」欄を追加し、自分が参加している
    他のプロジェクトへタスクを移動できるようにした。担当者・確認者の候補は、常に
    選択中プロジェクトのメンバーに絞り込まれる
  - 退会（アカウント削除）時、自分がオーナーで他にもメンバーがいるプロジェクトは、
    退会前に新オーナーへの譲渡が必須になった（`OwnershipHandoverSection.tsx`）。
    自分1人だけのオーナープロジェクトは、退会と同時にタスクごと削除される
  - 通知ベルが選択中プロジェクトに限らず、参加している全プロジェクトを横断して通知する
    ようになった。ヘッダーの通知ベルは直近6件のプレビューに変更し、全件は新規の
    通知専用画面（`NotificationsView.tsx`）で確認する構成にした
  - ヘッダーに選択中プロジェクト名を常時表示するバッジを追加
  - 「プロジェクト管理」タブのカード一覧が、タブレット幅でサイドバーを開いた際にボタン行の
    文字がはみ出して崩れる不具合を修正（コンテナクエリ方式へ変更。6.10参照）
  - データベース設計・RLSポリシーの詳細は`認証・DB設計書.md`11章を参照
- 2026-09-02：設定画面「アプリについて」タブの内容を更新（末尾の「開発：ユーザー名」表示を削除。
  `KEY_FEATURES`にプロジェクト管理・ゲストログインの項目を追加）。あわせてドキュメント全体を
  棚卸しし、`認証・DB設計書.md`11.5の誤り（`task_assignees`/`task_subtasks`のRLSポリシーが
  実際には未更新だった件）を発見。さらにユーザーからの指摘で、「メンバー別稼働状況」
  （`ProgressChart.tsx`）がプロジェクト絞り込み無しに全ユーザー（ゲストの残骸アカウント含む）を
  表示してしまう問題も発覚
- 2026-09-02（続き）：**上記2件を修正**。①`task_assignees`/`task_subtasks`のRLSポリシーを
  `tasks`本体と同じ「プロジェクトメンバーのみ」方式に揃え（`is_task_project_member(task_id)`。
  `supabase-migration-task-assignees-subtasks-rls-fix.sql`。詳細は`認証・DB設計書.md`
  11.5.1③）、②`App.tsx`に選択中プロジェクトのメンバーだけに絞り込んだ`currentProjectMembers`を
  追加し、`DashboardView`（`ProgressChart.tsx`・`GanttChart.tsx`）へ渡す`users`をこれに
  差し替えた。8章のTODO記載も修正済みに更新（詳細は`引継ぎメモ.md`参照）

---

## 1. これは何のアプリか

社内向けの、1人〜少人数チーム用タスク管理ダッシュボードです。Supabase（PostgreSQL + Auth）を
バックエンドに使っており、メールアドレス＋パスワードでサインアップした複数のユーザーが、
実際にログインして同じタスクを共有できます。

- 複数のプロジェクトを作成し、プロジェクトごとにタスク・メンバーを分けて管理できる
  （プロジェクト管理機能。6.11・7章参照）
- タスクをカンバン形式（未着手／進行中／査読・承認待ち／完了）で管理する
- ダッシュボードで進捗率・期限超過・メンバー負荷などを可視化する
- タスクには「確認者（レビュアー）」を設定でき、査読待ち→承認 or 差し戻し、という簡易ワークフローを持つ
- 配色テーマを12種類（ダーク6種／ライト6種、既定はGRAPHITE）の中から選べる
- 表示名・アバター画像・パスワードなど、自分のプロフィールを設定ページから変更できる（退会も可能）

現在の画面上のアプリ名は **WORK PLUS**（サイドバーロゴは `W+`）です。
公開URL：https://work-plus-eosin.vercel.app/

---

## 2. 技術スタック

| 項目 | 使用技術 |
|---|---|
| フレームワーク | React 19 |
| 言語 | TypeScript |
| ビルドツール | Vite |
| スタイリング | Tailwind CSS v4（`@theme`によるCSS変数ベースのカスタムテーマ） |
| バックエンド | Supabase（PostgreSQL + Auth + RLS + Storage）。ユーザー・タスク・アバター画像の実データを保持 |
| 状態の永続化 | ユーザー・タスクはSupabase（DB）に保存。配色テーマ・通知ON/OFF設定は個人のUI設定として引き続きブラウザの`localStorage`に保存 |
| ホスティング | Vercel |
| Lint | oxlint |
| テスト | Vitest + Testing Library（`npm run test`。7.1参照） |
| CI | GitHub Actions（push・PR時に`lint` → `test` → `build`を自動実行。7.2参照） |

`package.json` に定義された依存パッケージは `react` / `react-dom` / `@supabase/supabase-js` で、
状態管理ライブラリ（Redux等）やルーティングライブラリ（React Router等）は使っていません。画面遷移や
状態管理は素のReactの機能（`useState` / `useEffect` / `useMemo`）で、データの取得・保存は
`@supabase/supabase-js`経由のSupabase呼び出しで行っています。

---

## 3. ディレクトリ構成

```
task-dashboard/
├─ 規約.md                         … このプロジェクトのコーディングルール（AI向け・人向け共通）
├─ 認証・DB設計書.md               … Supabaseのスキーマ・RLS設計と実装状況をまとめた文書
├─ supabase-schema.sql             … Supabase側で1回実行するSQLマイグレーション（テーブル・トリガー・RLS）
├─ supabase-migration-profile.sql  … プロフィール機能用の追加マイグレーション（avatar_url列／avatarsストレージバケット）
├─ supabase-migration-account-deletion.sql … 退会機能用の追加マイグレーション（delete_own_account関数）
├─ supabase-migration-account-deletion-owner-handover.sql … 退会×オーナー引き継ぎ機能の追加マイグレーション（delete_own_account関数の拡張。11章参照）
├─ supabase-migration-projects.sql … プロジェクト管理機能用のマイグレーション（projects/project_membersテーブル・RLS一式。11章参照）
├─ supabase-migration-projects-rls-fix.sql / supabase-migration-projects-select-fix.sql … 上記実装時に発生したRLS不具合の履歴用ファイル（内容はsupabase-migration-projects.sqlに統合済み。11章参照）
├─ supabase-migration-task-assignees-subtasks-rls-fix.sql … task_assignees/task_subtasksのRLSポリシーをプロジェクトメンバー限定に修正するマイグレーション（11.5.1③参照）
├─ プロジェクト管理機能_要件定義書.md … プロジェクト管理機能の要件定義書
├─ .env.example                    … 環境変数のプレースホルダー（Gitにコミットする）
├─ .env                            … 実際のSupabase接続情報（Git管理外。各自で作成する）
├─ package.json
├─ vitest.config.ts                … Vitest（テスト実行）の設定。React用プラグイン・jsdom環境を指定
├─ .github/workflows/ci.yml        … GitHub Actions。push・PR時にlint→test→buildを自動実行
├─ src/
│  ├─ main.tsx                     … アプリの起動点。<App /> をHTMLにマウントするだけ
│  ├─ App.tsx                      … 状態をすべて持つルートコンポーネント（詳細は4章）
│  ├─ index.css                    … Tailwindのテーマ定義（12配色分のCSS変数）
│  ├─ vite-env.d.ts                … 環境変数（VITE_SUPABASE_URL等）の型定義
│  ├─ test/
│  │  └─ setup.ts                  … Vitestのグローバルセットアップ（jest-domのマッチャー拡張を読み込む）
│  ├─ constants/
│  │  └─ app.ts                    … アプリ名・バージョン・紹介文・公開URL等、複数箇所で共通参照する定数
│  ├─ types/
│  │  └─ task.ts                   … 型定義（Task, User, AppTheme など）
│  ├─ utils/
│  │  ├─ assignee.ts               … 担当者名の解決／タスク絞り込み（担当者・カテゴリ・優先度）の共通関数
│  │  ├─ assignee.test.ts          … assignee.tsのユニットテスト
│  │  ├─ date.ts                   … 「今日の日付」「期日までの残り日数」の共通計算関数（JST基準。
│  │  │                               TaskCard.tsx/KpiCards.tsx/TaskForm.tsx/App.tsx/GanttChart.tsx/
│  │  │                               ScheduleView.tsxの全画面がこの関数に統一済み）
│  │  └─ date.test.ts              … date.tsのユニットテスト（JST・年またぎの境界値を含む）
│  ├─ lib/
│  │  └─ supabaseClient.ts         … Supabaseクライアントのインスタンスを1つだけ生成するモジュール
│  ├─ pages/
│  │  ├─ Login.tsx                 … ログイン画面（Supabase Authによるサインイン／新規登録）
│  │  └─ ResetPassword.tsx         … パスワード再設定画面
│  ├─ components/
│  │  ├─ Sidebar.tsx               … 左側のナビゲーションメニュー（下部に設定ページへの入り口あり）
│  │  ├─ TaskForm.tsx              … タスク作成・編集モーダル（サブタスクの編集・プロジェクト移動もここで行う）
│  │  ├─ RejectReasonModal.tsx     … 差し戻し理由入力モーダル
│  │  ├─ ImageLightbox.tsx         … アバター画像クリック時の原寸表示ビューア（ヘッダーのアバター・
│  │  │                               設定ページのプロフィールアバター双方から利用する共通部品）
│  │  ├─ dashboard/
│  │  │  ├─ DashboardView.tsx      … ダッシュボード画面の入れ物
│  │  │  ├─ KpiCards.tsx           … 上部のKPIカード4枚
│  │  │  ├─ ProgressChart.tsx      … ステータス別グラフ＋メンバー負荷
│  │  │  └─ GanttChart.tsx         … 締切・タイムライン表
│  │  ├─ kanban/
│  │  │  ├─ KanbanBoard.tsx        … カンバンボード本体（4カラム）
│  │  │  ├─ TaskCard.tsx           … カンバン上のタスク1枚のカード（サブタスク進捗バッジ表示）
│  │  │  └─ TaskCard.test.tsx      … TaskCard.tsxのテスト（規約③遅延最優先ルールの表示優先順位を確認）
│  │  ├─ schedule/
│  │  │  └─ ScheduleView.tsx       … スケジュール画面（月間カレンダー＋祝日ハイライト。
│  │  │                               スマホ幅では縦1列のリスト表示に切り替わる）
│  │  ├─ project/                  … 【プロジェクト管理機能】（6.11参照）
│  │  │  ├─ ProjectManagementView.tsx … 「プロジェクト管理」タブ本体（一覧・検索/ステータス絞り込み）
│  │  │  ├─ ProjectFormModal.tsx   … プロジェクトの新規作成・編集モーダル
│  │  │  └─ MemberManagementModal.tsx … メンバー一覧・追加・削除・オーナー譲渡モーダル
│  │  ├─ notifications/            … 【プロジェクト管理機能】（6.2参照）
│  │  │  └─ NotificationsView.tsx  … 通知専用画面（全プロジェクト横断の通知を種類ごとに一覧表示）
│  │  └─ settings/
│  │     ├─ SettingsView.tsx       … 設定ページの入れ物（タブ切り替え。6.9参照）
│  │     ├─ ProfileSection.tsx     … 「プロフィール」タブ（表示名編集・アバター画像アップロード・パスワード変更）
│  │     ├─ DangerZoneSection.tsx  … 「データ」タブ内の退会（アカウント削除）機能
│  │     └─ OwnershipHandoverSection.tsx … 【プロジェクト管理機能】退会前のオーナー引き継ぎセクション（6.9・6.11参照）
│  │     ※「アプリについて」タブの中身はAboutSection.tsx（下記）
│  └─ assets/                      … react.svg / vite.svg / hero.png（現状未使用。スクリーンショットや
│                                      OGP画像等での利用を見込んで保持。2026-08-31にApp.css・
│                                      SettingsModal.tsxは削除済み）
```

なお`AboutSection.tsx`（設定ページ「アプリについて」タブ）は`src/components/settings/`配下に
あります。上記ツリーでは見落としやすいため、7章のファイル一覧表であらためて記載しています。

---

## 4. 状態管理の考え方（重要）

このアプリの一番の設計方針は **「状態はApp.tsxだけが持つ」** という点です（`規約.md` ではこれを
「Single Source of Truth（単一の真実の源泉）」と呼んでいます）。

```
App.tsx （すべての状態はここ）
 │
 ├─ session（Supabaseの認証セッション。nullならログイン画面を表示）
 ├─ authLoading（起動直後、セッション確認が終わるまでのローディング状態）
 ├─ users（Supabaseの`profiles`テーブルから取得したユーザー一覧。表示名・アバターURLを含む）
 ├─ tasks / tasksLoading（Supabaseの`tasks`テーブルから取得した、選択中プロジェクトのタスク一覧）
 ├─ notificationTasks（通知ベル判定専用。project_idで絞り込まない、全参加プロジェクトのタスク一覧）
 ├─ projects（参加中プロジェクトの一覧。RLSにより自分がメンバーの分だけ返る）
 ├─ currentProjectId（選択中プロジェクトのID。個人の選択状態としてlocalStorageに保存）
 ├─ projectMembers / projectTaskCounts（プロジェクトごとのメンバー一覧・タスク進捗集計）
 ├─ projectStatusFilter / projectSearchQuery（「プロジェクト管理」タブの絞り込み条件）
 ├─ isProjectFormOpen / editingProject / memberModalProjectId / isProjectMenuOpen
 │   （プロジェクトの作成・編集・メンバー管理モーダルやサイドバーアコーディオンの開閉状態）
 ├─ theme（配色テーマ。個人のUI設定として引き続きlocalStorage）
 ├─ currentView（今どの画面を表示中か）
 ├─ filterUser / filterCategory / filterPriority（絞り込み条件。全画面共通）
 ├─ notificationSettings（通知ベル4種類のON/OFF設定。個人のUI設定として引き続きlocalStorage）
 └─ rejectTargetId（差し戻し対象のタスクID）
       │
       │  Props（データを下に渡す）
       ▼
  Sidebar / DashboardView / KanbanBoard / ScheduleView / SettingsView / TaskForm / RejectReasonModal
  ProjectManagementView / ProjectFormModal / MemberManagementModal / NotificationsView …
       │
       │  コールバック関数（「こう変更して」とApp.tsxに依頼する）
       ▲
  onUpdateStatus / onProcessAction / onAddTask など
```

子コンポーネント（例：`TaskCard`）は自分でタスクの中身を書き換えることはせず、必ず
`onProcessAction(id, 'approve')` のように **親から渡された関数を呼び出す** ことで
「変更してほしい」とリクエストします。実際に `tasks` を書き換えるのはApp.tsxだけです。
これにより、データの流れが一方向になり、バグの原因を追いやすくなっています（Props / State の
基本については`学習ノート.md`2章を参照してください）。

`currentView`（`'dashboard' | 'tasks' | 'schedule' | 'settings' | 'project' | 'notifications'`）
という文字列1つで表示する画面を切り替えています。React Routerのような外部ルーティング
ライブラリは使わず、`{currentView === 'dashboard' ? <A/> : currentView === 'tasks' ? <B/> : <C/>}`
という条件分岐だけで画面遷移を実現しています（規約.mdで「一面集約型SPA」と呼ばれている設計です）。
「設定」画面（`'settings'`）もこの仕組みに乗っており、ヘッダー・サイドバーどちらの⚙️ボタンも
特別なモーダル用stateは持たず、単に`currentView`を`'settings'`に切り替えているだけです（6.9参照）。
「通知」専用画面（`'notifications'`）も同様に、サイドバーには項目を追加せず、通知ベルの
「すべて見る→」から`currentView`を切り替えるだけの入り方です（6.2参照）。

ダッシュボード／タスクボード／スケジュールの3画面は、選択中プロジェクト（`currentProjectId`）の
タスクだけをSupabaseから取得して表示します（`.eq('project_id', ...)`で絞り込み。6.11参照）。
未選択の間はこの3画面に「プロジェクトを選択してください」という案内が表示されます。

### 4.1 データの永続化

`users` / `tasks` / ログイン状態（`session`）は、すべてSupabase（PostgreSQL + Auth + Storage）に
保存されます。ページをリロードすると、Supabaseの`getSession()`でセッションを復元し、
`profiles`／`tasks`テーブルを改めて取得し直す仕組みです（詳細は6.1・6.2参照）。アバター画像は
Supabase Storageの`avatars`バケットに保存され、公開URLが`profiles.avatar_url`に記録されます。

一方、`theme`（配色テーマ）と`notificationSettings`（通知ベルのON/OFF設定）の2つだけは、
「この端末・このブラウザでの見た目の好み」という個人のUI設定と位置づけ、あえてSupabase化せず
引き続きブラウザの`localStorage`に保存しています（キー：`dashboard_theme` /
`dashboard_notification_settings`）。値が変わるたびに`useEffect`で保存され、ページをリロードしても
直前の状態が復元されます。

---

## 5. データモデル（型定義）

`src/types/task.ts` に定義されています。

### Task（タスク1件）

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | `string` | タスクの一意なID（`crypto.randomUUID()`で発行） |
| `title` | `string` | タスク名 |
| `description` | `string?` | 詳細説明（任意） |
| `status` | `TaskStatus` | 進行ステータス |
| `category` | `TaskCategory` | カテゴリ |
| `startDate` | `string` | 開始日（`YYYY-MM-DD`） |
| `endDate` | `string` | 期日（`YYYY-MM-DD`） |
| `priority` | `TaskPriority` | 優先度 |
| `assignees` | `string[]` | 担当者のUser ID配列（複数選択可。ただし0人にはできない） |
| `reviewerId` | `string?` | 確認者（レビュアー）のUser ID |
| `returnReason` | `string?` | 差し戻し理由（差し戻されたときだけ入る） |
| `createdBy` | `string` | 作成者のUser ID（削除ボタンの表示可否判定に使用。RLSの削除権限と揃える） |
| `subtasks` | `Subtask[]?` | サブタスク（チェックリスト）の配列（任意。無い場合は`undefined`） |
| `projectId` | `string` | 所属プロジェクトのID（NOT NULL）。`TaskForm.tsx`の「プロジェクト」欄で他プロジェクトへ移動できる（6.11参照） |

### Subtask（サブタスク＝タスク内の簡易チェックリスト項目）

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | `string` | サブタスクの一意なID（`crypto.randomUUID()`で発行） |
| `title` | `string` | サブタスクの内容 |
| `done` | `boolean` | 完了したかどうか |

サブタスクは、あくまで親タスクの中の「進捗メモ・チェックリスト」という位置づけです。
独自の期日は持たず、常に親タスクの期日（`endDate`）を共有します。また、`status`や
`returnReason`のような承認フロー（査読・差し戻し）も持たせていません。これは意図的な設計で、
「サブタスクごとに承認が要るのか？」という曖昧さを生まないよう、あえてシンプルな
チェックリストに留めています。

### 各種の値の種類（Union型）

| 型名 | 取りうる値 | 画面上の表示 |
|---|---|---|
| `TaskStatus` | `todo` / `doing` / `review` / `done` | 未着手／進行中／査読・承認待ち／完了 |
| `TaskCategory` | `開発` / `デザイン` / `マーケ` / `その他` | そのまま表示 |
| `TaskPriority` | `low` / `medium` / `high` | 低／中／高 |
| `AppTheme` | 下記12種類 | 設定ページのテーマタブに表示（英語表記のまま） |

`AppTheme`の12種類（既定は先頭の`graphite-dark`）：

| キー | 表示名 | 系統 |
|---|---|---|
| `graphite-dark` | GRAPHITE | ダーク（既定） |
| `sage-dark` | SAGE | ダーク |
| `bronze-dark` | BRONZE | ダーク |
| `ocean-dark` | OCEAN | ダーク |
| `amethyst-dark` | AMETHYST | ダーク |
| `lime-dark` | LIME | ダーク |
| `cream-light` | CREAM | ライト |
| `linen-light` | LINEN | ライト |
| `mist-light` | MIST | ライト |
| `pearl-light` | PEARL | ライト |
| `stone-light` | STONE | ライト |
| `sand-light` | SAND | ライト |

### User（担当者・レビュアー）

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | `string` | ユーザーID |
| `name` | `string` | 表示名 |
| `avatarUrl` | `string?` | アバター画像の公開URL（未設定の場合は`undefined`。イニシャル表示にフォールバック） |

Supabaseの`profiles`テーブル（`id` / `display_name` / `avatar_url`）から取得した実際のユーザー
一覧です。新規登録（サインアップ）すると、DB側のトリガー（`handle_new_user`）が自動的に
`profiles`へ1行追加するため、アプリ側で別途ユーザー登録の処理を書く必要はありません。

### Project（プロジェクト1件）

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | `string` | プロジェクトの一意なID |
| `name` | `string` | プロジェクト名 |
| `description` | `string?` | 説明（任意） |
| `status` | `ProjectStatus` | ステータス（進行中／完了／アーカイブ） |
| `createdBy` | `string` | 作成者のUser ID。オーナー自体はメンバーテーブル側のroleで管理する（6.11参照） |

`ProjectStatus`は`'active' | 'completed' | 'archived'`（進行中／完了／アーカイブ）の3値です。
デフォルトで一覧から隠れるのはアーカイブのみです。

メンバー一覧（誰がオーナー／メンバーか）・タスク進捗（完了数／総数）は`Project`型自体には
持たせず、`App.tsx`側で`Record<プロジェクトID, ...>`という別のstate（`projectMembers` /
`projectTaskCounts`）として保持しています（6.11参照）。

---

## 6. 画面・機能一覧

### 6.1 ログイン画面（`pages/Login.tsx`）

Supabase Authの`signInWithPassword` / `signUp`を直接呼び出す、実際に機能する認証画面です。
`App.tsx`側の`session`（Supabaseのセッション）が`null`のときだけこの画面が表示されます。

- 「サインイン」「新規登録」のタブで切り替えます。新規登録時は表示名も入力し、その値は
  DB側のトリガー経由で`profiles.display_name`に自動反映されます
- 新規登録が成功すると、メールアドレス確認が必須（Supabase Authのデフォルト設定）のため、
  「確認メールを送信しました」という案内画面に切り替わります。届いたメール内のリンクを
  クリックすると登録が完了し、サインインできるようになります
- サインイン成功後の画面遷移はこのコンポーネント自身では行いません。`App.tsx`側が
  Supabaseの`onAuthStateChange`を購読しており、セッションができた時点で自動的に
  ログイン後の画面へ切り替わります
- よくあるSupabaseのエラーメッセージ（英語）は、`translateAuthError`関数で日本語に
  変換して表示します（該当しないメッセージはそのまま表示）
- **ゲストログイン（2026-08-31追加）**：「ゲストとしてログイン」ボタンから、会員登録・
  パスワード入力なしでSupabaseの匿名認証（`signInAnonymously`）を使ってその場でログインできます。
  ポートフォリオサイト経由の訪問者向けの導線です。`Login.tsx`自身が行うのは
  `signInAnonymously()`の呼び出しだけで、ログイン成功後のデモ用プロジェクト・サンプルタスクの
  自動投入は`App.tsx`側（`seedGuestDemoData`）が担当します。これは、`Login.tsx`内で
  投入まで完結させると、`App.tsx`側のログイン検知（プロジェクト一覧の取得）と競合し、
  取得の方が先に終わって新規プロジェクトが画面に反映されないことがあったための設計です
  （認証・DB設計書.md12章参照）。ログアウト（サイドバー下部のログアウトボタン。6.3参照）すると、
  作成したデモデータ・匿名アカウント自体は自動的に削除されます

### 6.2 全体レイアウト（`App.tsx`）

- 左：サイドバー（開閉可能。スマホ幅ではオーバーレイ表示になり、項目選択と同時に自動で閉じる）
- 右上：共通ヘッダー（現在の画面名バッジ／選択中プロジェクト名バッジ／新規作成ボタン／通知ベル／
  設定ボタン／アバター）
- ヘッダー下：担当者・カテゴリ・優先度の絞り込みバー＋抽出件数（画面〈タブ〉ごとには分けず、
  全画面共通のフィルターとして扱う方針。スマホ幅では折り返して表示される。設定・プロジェクト管理・
  通知の3画面には表示されない）
- メイン：選択中の画面（ダッシュボード／タスクボード／スケジュール／プロジェクト管理／通知／設定）

配色テーマの切り替えは設定ページ（6.9参照）に集約されており、ヘッダーには単体のテーマ
ドロップダウンはありません。ヘッダーのアバター左の⚙️ボタンと、サイドバー下部のログアウト横の
⚙️ボタン、2箇所どちらからも同じ設定ページへ遷移できます。

選択中プロジェクトがある間は、ヘッダーの画面名バッジの隣にプロジェクト名のバッジが常時表示され
（フォルダアイコン付き。sm未満の画面幅では省略）、今どのプロジェクトを見ているか常に分かる
ようになっています（2026-08-29追加。6.11参照）。

#### 通知ベル

ヘッダー右側の🔔アイコンをクリックすると、ドロップダウンで通知一覧が開きます。専用のAPIは呼ばず、
`App.tsx`が保持する`notificationTasks`（選択中プロジェクトに絞らない、参加している全プロジェクトの
タスク。Supabaseから取得済みのもの）から毎レンダー時に導出するシンプルな仕組みです。
「自分が担当者になっているタスク」を対象に、以下の4種類を通知します
（`currentUserId`はSupabaseの認証セッションから取得した実際のユーザーIDです）。

| 種類 | 条件 |
|---|---|
| 遅延中 | 自分が担当者・未完了・期日が今日より過去 |
| 当日締切 | 自分が担当者・未完了・期日が今日 |
| 差し戻された | 自分が担当者・ステータスが`doing`・`returnReason`あり |
| 承認待ち | 自分が確認者（`reviewerId`）・ステータスが`review` |

通知は選択中プロジェクトに限らず、自分が参加している全プロジェクトを横断して表示されます
（2026-08-29対応。プロジェクト管理機能_要件定義書.md§4）。ドロップダウンは直近6件＋プロジェクト
名タグのプレビューに徹し、一番下の「すべて見る（n件）→」から通知専用画面
（`components/notifications/NotificationsView.tsx`・`currentView='notifications'`）へ遷移して
全件を確認します。専用画面では、種類ごと（遅延中／当日締切／差し戻された／承認待ち）に
セクション分けしてカード表示します。サイドバーには専用の項目は追加しておらず、通知ベルの
「すべて見る→」からのみ入る構成です（設定ページと同じ入り方）。

通知アイテムをクリックすると、そのタスクの編集モーダルがそのまま開きます。クリックしたタスクが
選択中でない別プロジェクトのものであれば、先に選択中プロジェクトをそのプロジェクトへ自動的に
切り替えてから開きます。件数はベルの右上にバッジ表示され（10件以上は「9+」表記）、0件のときは
バッジ自体が表示されません。

このアプリには通知テーブル・既読/未読の概念はなく、あくまで`tasks`の状態からその場で導出した
「今この瞬間の通知一覧」です（通知専用画面も過去ログを見る画面ではありません）。他ユーザーの
操作を拾うため、ログイン時・20秒ごとのポーリング・自分のタスク操作の直後に取得し直しています。

### 6.3 サイドバー（`components/Sidebar.tsx`）

4つのメニュー項目（上から**ダッシュボード**・**スケジュール**・**タスク一覧**・
**プロジェクト管理**の順。「プロジェクト管理」は2026-08-29にメニュー最下部へ移動）は、
すべて実際に機能します。

**プロジェクト管理**だけは他の項目と挙動が異なります（プロジェクト管理機能_要件定義書.md§2.1）。
クリックすると別画面へ即座に遷移するのではなく、その場でアコーディオン開閉し、
「＋新規プロジェクト」（一番上に固定）→参加中プロジェクトの一覧・選択（件数が増えても
スクロールに収まる）→「すべて管理→」（一覧性・管理操作のための専用タブへ遷移。6.11参照）の
順で表示します。ただしサイドバーが折りたたみ状態（アイコンのみ）のときは、展開表示する
スペースが無いため、クリックで直接プロジェクト管理タブへ遷移する簡易挙動になります。

フッター（ログアウトボタンの隣）には設定ページへの⚙️ボタンもあります。メインの4項目とは
見た目上分けて配置していますが、動作としては同じ`currentView`切り替えの一種で、設定ページを
表示中は他のメニュー項目と同じようにハイライト表示されます（6.9参照）。

### 6.4 ダッシュボード画面（`components/dashboard/`）

`DashboardView.tsx` が入れ物となり、以下3つを縦に並べています。

1. **KpiCards**：完了率／タスク進捗（完了数/総数）／査読待ち／期限超過、の4枚のカード
2. **ProgressChart**：
   - 左：ステータス別（未着手・進行中・査読待ち・完了）の件数を積層バーで表示
   - 右：メンバーごとの担当タスク数をバーで表示
3. **GanttChart（締切・タイムライン）**：
   - 今日を中心に「2日前〜4日後」の7日分を横並びで表示（「今日」は`src/utils/date.ts`の
     `getTodayJstDateString`で判定）
   - 完了していない・期日のあるタスクのうち、期日が近い順に最大5件を表示
   - 期日超過のタスクは常に赤い「遅延」バッジが最優先で表示される
     （規約.mdの「遅延最優先ルール」：他のどんな状態よりも遅延表示を優先する）

いずれも、ヘッダーの「担当者・カテゴリ・優先度」絞り込みバーと連動して表示内容が変わります。

### 6.5 タスクボード画面（`components/kanban/`）

`KanbanBoard.tsx` が4つのカラム（未着手／進行中／査読・承認待ち／完了）を描画し、
`TaskCard.tsx` が各カード（タスク1件）を描画します。

- カードはドラッグ＆ドロップでカラム間を移動できます
- 例外：**査読・承認待ち→進行中** にドラッグした場合だけ、直接は移動せず
  「差し戻し理由モーダル」が開きます（理由を入力して初めて進行中に戻る）
- カードをクリックすると編集モーダル（TaskForm）が開きます
- カード下部のボタンはステータスに応じて変化します：
  - `進行中` のとき：「承認申請」ボタン（→ 査読・承認待ちへ）
  - `査読・承認待ち` のとき、確認者本人には「差し戻し」ボタン（→ 理由入力モーダルへ）／
    「承認完了」ボタン（→ 完了へ）。確認者本人以外には操作ボタンの代わりに「承認待ち」という
    バッジのみを表示する（自分が操作できないことが分かるように）
  - 常に：「削除」ボタン
- 期日超過（遅延・赤枠）・期日3日以内（黄枠・パルス点滅）・差し戻し中（薄い赤枠）の
  3種類の強調表示があり、**遅延が常に最優先**で表示されます
- カラムはコンテナクエリ（`@min-[1040px]:`。App.tsxの`<main>`に付けた`@container`基準）で
  「実際にこのボードへ残っている横幅」を判定し、十分な横幅があれば4カラムの等分グリッド、
  無ければモバイルと同じ横スクロール式の固定幅カラム表示に切り替わります。以前はビューポート幅
  （`md:`）だけで判定していたため、タブレット幅でサイドバーを開くと実際の残り幅が足りず
  カラムが極端に狭くなり崩れる不具合があった（6.10参照）

### 6.6 スケジュール画面（`components/schedule/ScheduleView.tsx`）

月間カレンダー形式で、タスクの期日（`endDate`）を日付ごとに表示する画面です。

- ヘッダーの「前の月／今日／次の月」ボタンで表示月を切り替えられます（日曜始まり、
  前後の月の余白日を含めて7列×N週のマス目を生成）
- 日本の祝日を外部API（[holidays-jp](https://holidays-jp.github.io/)）から取得し、該当する
  日のマスを赤くハイライトして祝日名を表示します。マウント時（画面表示時）に1回だけ取得し、
  以降は使い回します。取得に失敗した場合でも、カレンダー自体は問題なく表示され、「祝日データの
  取得に失敗したため、祝日のハイライトなしで表示しています」という注記だけが出ます（祝日API
  はこのアプリで唯一の外部API連携で、秘密のAPIキーが不要な公開APIです）
- 各マスには、その日が期日のタスクが最大3件まで表示され（超える分は「+◯件」の表記）、
  クリックするとそのタスクの編集モーダルが開きます。タスクの色分けは、完了（緑）／期日超過（赤）／
  それ以外（テーマカラー）の3種類です
- ヘッダーの「担当者・カテゴリ・優先度」絞り込みバーと連動して表示内容が変わります（他の画面と
  同じ共通フィルター`filterTasks`を使用）
- 「今日」の判定は、他の画面（TaskCard.tsx等）と同じ`src/utils/date.ts`の`getTodayJstDateString`
  （JST基準）で行っています
- 実際に使える横幅が狭いときはレイアウトが変わります：7列の月間グリッドのままだと1マスが
  狭すぎるため、日付を縦1列に並べる「リスト表示」に切り替わります。曜日の見出し行は非表示にし、
  代わりに各日付の横に曜日を直接添えます（例：「22 (土)」）。前後の月の余白日もリスト表示では
  非表示にし、当月分の日付だけを表示します（十分な横幅があれば、従来通り7列のグリッド表示のままです）。
  この判定はコンテナクエリ（`@min-[640px]:`。カンバンと同じApp.tsxの`<main>`の`@container`基準）で
  行っており、ビューポート幅ではなく実際にこの画面へ残っている横幅で判定するため、サイドバーの
  開閉状態によらず正しく切り替わります（6.10参照）

### 6.7 タスク作成・編集モーダル（`components/TaskForm.tsx`）

- タスク名／詳細説明／サブタスク（チェックリスト）／作業担当者／確認者（レビュアー）／
  カテゴリ／優先度／期日、を入力
- **サブタスク（チェックリスト）を追加できます**。タイトルを入力して追加ボタンを押すと
  一覧に加わり、チェックボックスで完了・未完了を切り替えたり、個別に削除したりできます。
  サブタスクは親タスクと同じ期日を共有し、独自の承認フローは持ちません（5章参照）。
  カンバンのカードには「完了数/総数」の進捗バッジが表示されます（6.5参照）
- **作業担当者はチェックボックスで複数選択できます**。新規作成時は自分のみが選択された状態が初期値で、
  他のユーザーも自由に追加・除外できます（最低1人は必ず選択されている必要があり、
  残り1人の状態からは解除できません）
- 確認者（レビュアー）は、**作業担当者に選ばれていないユーザー**の中から選べます
  （担当者に選んだ人を確認者にも選んでしまう＝自分で自分の作業を承認できてしまう状態を防ぐため、
  候補から自動的に除外されます）
- 作業担当者・確認者の選択候補は、常に**選択中プロジェクトのメンバーのみ**に絞り込まれます
  （プロジェクト機能導入前は登録ユーザー全員が対象でした。2026-08-29変更。6.11参照）
- **編集時のみ、「プロジェクト」欄が表示されます**。自分が参加している他のプロジェクトへ
  タスクを移動できます（プロジェクト管理機能_要件定義書.md§2.5）。プロジェクトを変更すると、
  移動先のメンバーでなくなる担当者・確認者だけが自動的に選択から外れます（全リセットではなく、
  該当者だけをクリア）
- 編集時は既存の値がフォームに復元されます
- 新規作成時、開始日・期日は「今日」（JST基準）が初期値として入ります（`src/utils/date.ts`の
  `getTodayJstDateString`を使用）
- 選択した担当者は、保存時にSupabaseの`task_assignees`テーブルへ実際に反映され、他のユーザーとも
  共有されます（保存の都度、既存の担当者・サブタスク行をいったん全削除してから作り直す方式。
  `認証・DB設計書.md`10章参照）
- タスクの作成者（`createdBy`）は、フォームからは渡さずApp.tsx側で挿入時に`currentUserId`から
  設定します（5章参照）
- **既知の制約**：確認者（レビュアー）の候補は「選択中プロジェクトのメンバーのうち、
  担当者に選ばれていないユーザー」から絞り込むため、プロジェクトのメンバーが少ないうち
  （特に自分1人だけの場合）は候補が0人になり、確認者欄が空欄のままタスクを作成することに
  なります（8章参照）

### 6.8 差し戻し理由モーダル（`components/RejectReasonModal.tsx`）

- 差し戻し理由を自由入力できるテキストエリア
- テキストエリアには `required`（HTML標準の必須入力チェック）が付いているため、
  何も入力しないままでは、ブラウザが送信自体をブロックします（「差し戻しを確定」ボタンを押しても
  何も起きません）
- 送信時は`trim()`後に空文字であれば送信自体を中断する（`if (!trimmed) return;`）方式にしており、
  スペースのみの入力で送信されてしまうケースも防いでいます。これにより「差し戻し理由は必ず書く」
  という挙動が、通常入力・空白のみ入力のどちらのケースでも一貫して保証されます

### 6.9 設定ページ（`components/settings/`）

ダッシュボードやタスクボードと同じ「一面表示」の画面です（`currentView === 'settings'`）。
ヘッダーのアバター左の⚙️ボタンと、サイドバー下部のログアウト横の⚙️ボタン、2箇所どちらから
遷移しても同じこのページが表示されます。開いている間は、遷移元の⚙️ボタンの側がハイライト表示されます。

`SettingsView.tsx`が入れ物となり、上部のタブ切り替えで以下5つのセクションを出し分けます
（縦スクロールが長くなりすぎないよう、カテゴリごとにタブで分割しています）。

1. **プロフィール**（`ProfileSection.tsx`）：
   - 表示名の編集（`profiles.display_name`を更新）
   - アバター画像のアップロード（Supabase Storageの`avatars`バケットへ保存し、`profiles.avatar_url`
     を更新。ヘッダー・カード等のアバターアイコンは丸型で表示され、画像未設定時は表示名の頭文字に
     フォールバックする）。画像が設定されている場合、アバターをクリックすると`ImageLightbox`
     （3章参照）で原寸表示できる（X/Instagramのプロフィール画像タップと同じ挙動のイメージ）。
     同じ挙動はヘッダー右上のアバターからも利用できる
   - パスワード変更（現在のパスワードで再認証してから`supabase.auth.updateUser`を呼び出す）
2. **テーマ**：12種類の配色テーマ（5章参照）から選択できます。各テーマの実体は`src/index.css`の
   `@theme`ブロックで定義されたCSS変数（`--theme-accent`など）で、選択すると
   `<html data-theme="...">`属性が切り替わり、対応する配色が全体に反映されます
3. **通知**：通知ベルの4種類（遅延中／当日締切／差し戻された／承認待ち。6.2参照）を、それぞれ
   個別にON/OFFできます。OFFにした種類は、以降ヘッダーの通知ベルに一切表示されなくなります。
   設定は`localStorage`（キー：`dashboard_notification_settings`）に保存され、デフォルトは全種類ONです
4. **データ**：
   - サンプルデータへのリセット：自分（ログイン中のユーザー）が作成したタスクを削除し、初期の
     サンプルタスクへリセットするボタン。他ユーザーが作成したタスクには影響しません
   - **オーナー引き継ぎ（`OwnershipHandoverSection.tsx`。2026-08-29追加）**：自分がオーナーで、
     かつ他にもメンバーがいるプロジェクトが1件以上残っている間は、退会ボタンの代わりにこちらが
     表示されます。プロジェクトごとに新オーナーを選ぶプルダウン＋「譲渡する」ボタンが並び、
     すべて譲渡し終えると自動的に退会ボタン（下記）へ切り替わります（プロジェクト管理機能_
     要件定義書.md§6.2・6.11参照）
   - 退会（`DangerZoneSection.tsx`）：パスワードを再入力のうえ確認ダイアログを通過すると、
     自分が作成したタスクの削除・自分が確認者に設定されているタスクの`reviewerId`のクリア・
     `auth.users`からの自分のアカウント削除までを、Supabase側の`security definer`関数
     `delete_own_account`（`supabase-migration-account-deletion.sql`で追加）経由でまとめて行う。
     自分がオーナーで他に誰もメンバーがいない（＝自分1人だけの）プロジェクトは、この時に
     タスクごとまとめて削除される（6.11参照）
5. **アプリについて**（`AboutSection.tsx`）：アプリ名・バージョン・紹介文・技術スタック・
   主な機能・GitHubリポジトリと公開URLへのリンクをまとめた紹介カード。社内の共有会などで
   このアプリを軽く紹介する際に、画面をそのまま見せれば説明が済むことを目的にしている。
   表示内容は`src/constants/app.ts`の定数を参照している

いずれのタスク・アカウント操作も、パスワード再認証や確認ダイアログ（`window.confirm`）を
挟むかどうかは操作の重さに応じて使い分けています（パスワード変更・退会は再認証必須、
サンプルデータリセットは確認ダイアログのみ）。

旧実装だった`SettingsModal.tsx`は2026-08-31に削除済みです（3章参照）。

### 6.10 モバイル（スマホ）・タブレット対応

アプリ全体でTailwindのブレークポイント（`sm:` / `md:` 等）を使い、スマホ幅での表示を個別に調整しています。

- サイドバーはスマホ幅ではオーバーレイ表示になり、項目を選択すると同時に自動で閉じます
- メインビュー領域の高さは固定値ではなく `flex-1 min-h-0` で計算しており、ヘッダーやフィルターバーの
  実際の高さ（スマホ幅で折り返して増える等）に関わらず、残りの領域を正しく埋めます
- フィルターバー（担当者・カテゴリ・優先度）や設定ページのタブ選択部分は、横はみ出しせず
  `flex-wrap`で自然に折り返します（横スクロールバーを出さない、というこのアプリ共通の方針）
- タスク作成・編集モーダル（`TaskForm.tsx`）のカテゴリ／優先度／期日の3項目は、スマホ幅では
  横3列ではなく縦積みで表示されます
- スケジュール画面（カレンダー）は、単にグリッドを縮小するのではなく、日付を縦1列に並べる
  「リスト表示」に構造そのものを切り替えます（6.6参照）
- **タスクボード（6.5）・スケジュール（6.6）・プロジェクト管理タブ（6.11）は、`sm:` / `md:`
  のようなビューポート幅のブレークポイントではなく、コンテナクエリ（`@container` /
  `@min-[…]:`）でレイアウトを切り替えています**。理由：これらの画面はどれも「横に何列も
  並べる」レイアウトのため、実際に使える横幅（サイドバーが開いていれば、その分だけ狭くなる）が
  直接崩れやすさに関わります。ビューポート幅だけを見るブレークポイントでは、ビューポート自体は
  `md`以上でもサイドバーを開いた分だけ実際の残り幅が足りない、というケース（タブレット幅で
  サイドバーを開いたとき等）を判定できず、カラムやカードが極端に狭くなって崩れる不具合があった。
  App.tsxの`<main>`に`@container`クラスを付け、その実際の横幅を基準に`KanbanBoard.tsx`・
  `ScheduleView.tsx`・`ProjectManagementView.tsx`側で`@min-[1040px]:` / `@min-[640px]:`のように
  判定することで解消した（Tailwind CSS v4はコンテナクエリを標準搭載しており、プラグイン追加は
  不要）。`ProjectManagementView.tsx`のカード内ボタン行（削除／メンバー管理／編集／開く）には、
  念のため`flex-wrap`も追加してあり、それでも幅が足りない場合はボタンが潰れず2行に折り返す

なお、このセッションのサンドボックスには実機・ブラウザでの見た目検証ができる環境が無いため、
上記の対応はコードレビューベースで実施しています。実機・DevToolsでの見た目確認は開発者側で行ってください。

### 6.11 プロジェクト管理機能（`components/project/` 他）

2026-08-29に実装。複数のプロジェクトを作成し、プロジェクトごとにタスク・メンバーを分けて
管理できるようにする機能です（詳細な要件は`プロジェクト管理機能_要件定義書.md`、DB設計は
`認証・DB設計書.md`11章を参照）。

#### プロジェクトの選択・切り替え

- サイドバーのアコーディオン（6.3参照）から素早く切り替えられます。選択すると自動的に
  ダッシュボードへ遷移します
- 選択中プロジェクトのIDは、テーマと同様に個人の選択状態としてlocalStorageに保存され、
  リロードしても直前に見ていたプロジェクトが復元されます
- ダッシュボード／タスクボード／スケジュールの3画面は、選択中プロジェクトのタスクだけを
  表示します。未選択の間はこの3画面に案内が表示されます
- 参加プロジェクト一覧を取得し直した結果、選択中プロジェクトがその一覧に含まれなくなっていた
  場合（メンバーから外れた・削除された等）は、自動的に選択状態が解除されます

#### 「プロジェクト管理」タブ（`ProjectManagementView.tsx`）

- 参加プロジェクトをカード形式で一覧表示します。各カードには、自分のロール
  （オーナー／メンバー）・メンバー数・タスク進捗％（完了数／総数。`KpiCards.tsx`と同じ
  計算式）を表示します
- 検索ボックス（プロジェクト名の部分一致。全角/半角の違いはNFKC正規化で同一視）と
  ステータスタブ（すべて／進行中／完了／アーカイブ）で絞り込めます。「すべて」タブは
  アーカイブ以外の全件を指し、アーカイブ済みは専用タブからのみ見られます
- 「開く」ボタンで該当プロジェクトを選択中に切り替えます。「編集」「メンバー管理」「削除」は
  オーナーのみに表示されます。オーナー以外のメンバーには、代わりに「抜ける」ボタンが
  表示されます（オーナー本人は他の誰かへ譲渡するまで抜けられません）
- 新規作成・編集は`ProjectFormModal.tsx`（プロジェクト名・説明・ステータスを入力）で行います

#### メンバー管理・オーナー譲渡（`MemberManagementModal.tsx`）

- 現在のメンバー一覧（アバター・名前・ロールバッジ）を表示し、オーナーは非オーナー行に
  「オーナーにする」「削除」ボタンを、未参加の登録ユーザーには「追加」ボタンを使えます
- オーナー譲渡を実行すると、実行した本人はメンバーに降格します（Supabase側の
  `transfer_project_ownership`関数を経由。11章参照）

#### タスクのプロジェクト間移動（`TaskForm.tsx`）

6.7参照。編集画面にのみ「プロジェクト」欄があり、自分が参加している他のプロジェクトへ
タスクを移動できます。作業担当者・確認者の選択候補は、常に選択中プロジェクトのメンバーに
絞り込まれます。

#### 退会時のオーナー引き継ぎ

6.9参照。自分がオーナーで他にもメンバーがいるプロジェクトは、退会前に新オーナーへの譲渡が
必須です。自分1人だけのオーナープロジェクトは、退会と同時にタスクごと削除されます。

#### 通知の全プロジェクト横断対応

6.2参照。通知ベルは選択中プロジェクトに限らず、参加している全プロジェクトを横断して通知します。

---

## 7. ファイル一覧（役割まとめ）

| ファイル | 役割 |
|---|---|
| `supabase-schema.sql` | Supabase側で実行するSQLマイグレーション（テーブル・トリガー・RLSポリシー一式） |
| `supabase-migration-profile.sql` | プロフィール機能用の追加マイグレーション（`profiles.avatar_url`列・`avatars`ストレージバケット・RLSポリシー） |
| `supabase-migration-account-deletion.sql` | 退会機能用の追加マイグレーション（`delete_own_account`関数） |
| `supabase-migration-projects.sql` | プロジェクト管理機能用のマイグレーション（`projects`/`project_members`テーブル・トリガー・`transfer_project_ownership`関数・RLS一式） |
| `supabase-migration-projects-rls-fix.sql` / `supabase-migration-projects-select-fix.sql` | プロジェクト管理機能の実装時に発生したRLS不具合の履歴用ファイル（内容は`supabase-migration-projects.sql`に統合済み。11章参照） |
| `supabase-migration-account-deletion-owner-handover.sql` | 退会×オーナー引き継ぎ機能の追加マイグレーション（`delete_own_account`関数へ、自分1人だけのオーナープロジェクトを削除するステップを追加） |
| `supabase-migration-task-assignees-subtasks-rls-fix.sql` | `task_assignees`/`task_subtasks`のRLSポリシーを`tasks`本体と同じ「プロジェクトメンバーのみ」方式に修正するマイグレーション（`is_task_project_member`関数を新設。11.5.1③参照） |
| `supabase-migration-guest-login.sql` | ゲストログイン機能用の追加マイグレーション（`handle_new_user`関数を、匿名ユーザー〈emailがNULL〉でも`profiles.display_name`が作れるように修正。12章参照） |
| `プロジェクト管理機能_要件定義書.md` | プロジェクト管理機能の要件定義書 |
| `.env.example` / `.env` | Supabaseの接続情報（Project URL・anon key）。`.env`はGit管理外 |
| `認証・DB設計書.md` | Supabaseのスキーマ・RLS設計と実装状況をまとめた文書 |
| `src/main.tsx` | アプリの起動点。`<App />` をDOMにマウントするだけ |
| `src/App.tsx` | 全状態を保持するルートコンポーネント。画面のレイアウトも組み立てる |
| `src/vite-env.d.ts` | 環境変数（`VITE_SUPABASE_URL`等）の型定義 |
| `src/constants/app.ts` | アプリ名・バージョン・紹介文・公開URL等、複数箇所で共通参照する定数 |
| `src/lib/supabaseClient.ts` | Supabaseクライアントのインスタンスを1つだけ生成するモジュール |
| `src/types/task.ts` | 型定義（Task / User / AppTheme 等） |
| `src/utils/assignee.ts` | 担当者名の解決・タスク絞り込み（担当者・カテゴリ・優先度）の共通関数（`filterTasks`。重複コードを一本化したもの） |
| `src/utils/date.ts` | 「今日の日付」「期日までの残り日数」をJST基準・タイムゾーン非依存で計算する共通関数（TaskCard.tsx・KpiCards.tsx・TaskForm.tsx・App.tsx・GanttChart.tsx・ScheduleView.tsxで共用） |
| `src/pages/Login.tsx` | ログイン画面（Supabase Authによるサインイン／新規登録） |
| `src/components/Sidebar.tsx` | 左ナビゲーションメニュー（下部に設定ページへの入り口あり） |
| `src/components/TaskForm.tsx` | タスク作成・編集モーダル |
| `src/components/RejectReasonModal.tsx` | 差し戻し理由入力モーダル |
| `src/components/ImageLightbox.tsx` | アバター画像クリック時の原寸表示ビューア（ヘッダー・プロフィールタブ共通） |
| `src/components/dashboard/DashboardView.tsx` | ダッシュボード画面の入れ物 |
| `src/components/dashboard/KpiCards.tsx` | KPIカード4枚 |
| `src/components/dashboard/ProgressChart.tsx` | ステータス別グラフ＋メンバー負荷グラフ |
| `src/components/dashboard/GanttChart.tsx` | 締切・タイムライン表 |
| `src/components/kanban/KanbanBoard.tsx` | カンバンボード本体 |
| `src/components/kanban/TaskCard.tsx` | カンバンのタスクカード1枚（サブタスク進捗バッジ表示） |
| `src/components/schedule/ScheduleView.tsx` | スケジュール画面（月間カレンダー＋祝日API連携。スマホ幅では縦1列リスト表示） |
| `src/components/project/ProjectManagementView.tsx` | 「プロジェクト管理」タブ本体（カード一覧・検索/ステータス絞り込み。11章参照） |
| `src/components/project/ProjectFormModal.tsx` | プロジェクトの新規作成・編集モーダル |
| `src/components/project/MemberManagementModal.tsx` | メンバー一覧・追加・削除・オーナー譲渡モーダル |
| `src/components/notifications/NotificationsView.tsx` | 通知専用画面（全プロジェクト横断の通知を種類ごとに一覧表示。6.2参照） |
| `src/components/settings/SettingsView.tsx` | 設定ページの入れ物（プロフィール／テーマ／通知／データ／アプリについての5タブ切り替え） |
| `src/components/settings/ProfileSection.tsx` | 「プロフィール」タブ（表示名編集・アバター画像アップロード・パスワード変更） |
| `src/components/settings/DangerZoneSection.tsx` | 「データ」タブ内の退会（アカウント削除）機能 |
| `src/components/settings/OwnershipHandoverSection.tsx` | 「データ」タブ内のオーナー引き継ぎセクション（退会前に表示。6.9・11章参照） |
| `src/components/settings/AboutSection.tsx` | 「アプリについて」タブ（アプリ紹介・技術スタック・主な機能・リンク） |
| `src/index.css` | Tailwind v4のテーマ定義（12配色） |
| `src/assets/*` | react.svg / vite.svg / hero.png。現状未使用だが、スクリーンショットやOGP画像等での利用を見込んで保持 |
| `src/pages/ResetPassword.tsx` | パスワード再設定画面 |
| `src/utils/date.test.ts` / `src/utils/assignee.test.ts` | それぞれのユーティリティのユニットテスト |
| `src/components/kanban/TaskCard.test.tsx` | 規約③遅延最優先ルールの表示優先順位を確認するテスト |
| `src/test/setup.ts` | Vitestのグローバルセットアップ |
| `vitest.config.ts` | Vitestの設定（Reactプラグイン・jsdom環境） |
| `tsconfig.test.json` | テストファイル専用の型チェック設定。`npm run build`のtsc対象からは独立させ、エディタでの型解決のために`tsconfig.json`の`references`に追加 |
| `.github/workflows/ci.yml` | GitHub Actions。push・PR時にlint→test→buildを自動実行 |

### 7.1 テスト（Vitest + Testing Library）

全画面・全機能を網羅するのではなく、**バグに気づきにくく、間違えると実害が大きい部分**に絞って
最低限のユニットテストを用意しています。

- `src/utils/date.test.ts`：`getDaysDiffFromToday`のテスト。今日・未来・過去（期日超過）・
  年をまたぐ場合の差分日数が正しいことを確認する（タイムゾーンに関わるバグは見た目では
  気づきにくいため）
- `src/utils/assignee.test.ts`：`resolveAssigneeName`（ID／名前どちらからでも解決できるか）と
  `filterTasks`（担当者・カテゴリ・優先度の組み合わせ、旧データ形式への後方互換フォールバック）
  のテスト
- `src/components/kanban/TaskCard.test.tsx`：**規約③「遅延最優先ルール」**の回帰テスト。
  「差し戻し中（doing・差し戻し理由あり）」かつ「期日超過」の両方に該当するタスクをレンダリングし、
  差し戻し用の枠線スタイルではなく、遅延（最優先）の枠線スタイルが適用されることを確認する。
  他の状態のUIが遅延表示を上書きしていないかを検知する、このアプリで最も壊れてはいけない
  ロジックへのテスト

`npm run test`で実行します（`vitest run`のエイリアス）。設定は`vitest.config.ts`と
`src/test/setup.ts`（jest-domのマッチャー拡張読み込み・各テスト後のDOM自動cleanup）。

テスト対象ファイル（`src/**/*.test.ts` / `*.test.tsx` / `src/test/`）は、`tsconfig.app.json`
では`exclude`し、代わりに専用の`tsconfig.test.json`で型チェックする構成にしています。
理由は2つです。

1. `npm run build`（`tsc -b tsconfig.app.json tsconfig.node.json && vite build`）は
   `tsconfig.test.json`を対象に含めないため、テストコード側の型の問題で本番ビルドが
   止まることはありません
2. とはいえテストを`tsconfig.app.json`から完全に除外したままだと、エディタ（VS Code等）が
   そのファイルをどのtsconfigにも属さない扱いにしてしまい、`toBeInTheDocument`のような
   jest-domの拡張マッチャーが型エラーとして表示される問題がありました。`tsconfig.test.json`を
   ルートの`tsconfig.json`の`references`に加えることで、エディタ上は正しく型解決されつつ、
   ビルドコマンドからは独立させています

### 7.2 CI（GitHub Actions）

`.github/workflows/ci.yml`で、`main`ブランチへのpushとPull Requestのたびに、Node.js環境で
`npm ci` → `npm run lint` → `npm run test` → `npm run build`を自動実行します。このAIアシスタントの
作業環境では`npm install`・`npm run build`が実行できないため、実際のビルド確認はこれまで
ユーザー自身がローカルで行う必要がありましたが、CIによってpush後に自動でも確認できるように
なりました。

---

## 8. 今後の実装予定・既知の制約（TODO）

現時点で分かっている「未完成な部分」や「今後の拡張ポイント」をまとめておきます。

- **Supabase実装のうち、実機での動作確認がまだ済んでいない部分がある**：サインアップ・
  サインイン・タスクの新規作成は実機で確認済みですが、タスクの編集・削除・承認フロー
  （申請／承認／差し戻し）・通知ベル・サブタスクのSupabase連携・設定ページのサンプルデータ
  リセットは、今後順次確認していく予定です（`認証・DB設計書.md`10章参照）。
- **プロフィール・退会機能用の追加マイグレーションが未実行の可能性がある**：
  `supabase-migration-profile.sql`（アバター列・Storageバケット）と
  `supabase-migration-account-deletion.sql`（`delete_own_account`関数）は、Supabase側の
  SQL Editorで実行済みかどうかを別途確認する必要があります。未実行の場合、アバター
  アップロードと退会機能は失敗します。
- **担当者・確認者の選択UIを「検索付き」に発展させる案**：現状はチェックボックス／選択式で
  実装されています。登録ユーザーが増えてきたタイミングで、入力しながら絞り込む検索付き
  コンボボックス（オートコンプリート）に作り替える方針です（着手はまだ）。
- **担当者を全員選ぶと確認者の候補が0人になる**：`TaskForm.tsx` の確認者（レビュアー）候補は
  「担当者に選ばれていないユーザー」から絞り込む仕組みのため、登録ユーザーが少ないうち
  （特に自分1人だけの場合）は確認者の選択肢が無くなります。実際のユーザー登録が増えるまでは、
  確認者欄が空欄のままタスクを作成することになります（想定通りの挙動。6.7参照）。
- **Supabase Realtime対応は未着手**：他ユーザーの操作がリアルタイムで画面に反映される機能は、
  `認証・DB設計書.md`8章で「任意」と位置づけられており、明示的な着手指示が出るまでは着手しません。
- **退会×オーナー引き継ぎの実退会テストが保留中**：オーナー引き継ぎセクションが正しく表示される
  ことは確認済みですが、①実際に譲渡ボタンを押して完了させる、②退会を最後まで実行して自分1人
  だけのプロジェクトが実際に削除されることの確認、の2点は「テストが大変なので後で」という
  理由でユーザーが意図的に保留しています（バグではありません。11章参照）。
- **タスク内のコメント／チャットのようなやり取り機能（構想段階、未着手）**：まだ要件定義は
  行っていません。実現した場合、新しい通知の種類が増えることが見込まれるため、通知ベルの
  実装（`NotificationType`・`NotificationsView.tsx`の`TYPE_SECTIONS`・`SettingsView.tsx`の
  ON/OFF設定）は種類を追加しやすい設計にとどめてあります。
- **組織全体の管理者ロールは未実装（設計のみ）**：プロジェクト単位のオーナー／メンバーロールは
  実装済みですが、組織全体を横断する管理者ロールはYAGNI判断で見送っています
  （プロジェクト管理機能_要件定義書.md§7.3）。
- **祝日API（外部API連携）への依存**：スケジュール画面は`holidays-jp`の祝日APIに依存しています。
  秘密キー不要の公開APIのため現状は直接ブラウザから呼び出していますが、取得に失敗しても
  カレンダー自体の表示は止まらないようフォールバック済みです（6.6参照）。今後、秘密のAPIキーが
  必要な外部APIを使う機能（例：天気API等）を追加する場合は、キーをブラウザに露出させないよう、
  Supabase Edge Functionsなど秘密鍵をサーバー側に隠す仕組みを別途整えてから実装する方針です
  （現状のSupabase実装はDBアクセス・Auth・Storageのみで、Edge Functionsはまだ導入していません）。
- **サブタスクは承認フローを持たない簡易チェックリスト**：親タスクの承認・差し戻しフローとは
  独立しており、サブタスク単体の承認や個別期日はありません（5章・6.7参照）。将来、
  サブタスクごとに進捗以上の管理（担当者・期日など）が必要になった場合は、改めて設計し直す
  想定です。
- **`App.css`・`components/SettingsModal.tsx`は2026-08-31に削除済み**：Viteの初期テンプレートの
  残骸と、設定機能をページ化（settings/SettingsView.tsxへ移設）した際に不要になった旧実装で、
  いずれもどこからも参照されていないことを確認済みでした。`src/assets/`の3ファイル
  （react.svg / vite.svg / hero.png）は、スクリーンショットやOGP画像等に使う可能性があるため
  現状は保持しています（未使用のままなら将来削除候補）。
- **`KpiCards.tsx` の `totalTasksCount` propが未使用**：`DashboardView.tsx` から渡されていますが、
  `KpiCards.tsx` 内で実際には使われていません。将来的に削除候補です。
- **タスク・ユーザーデータはSupabaseに保存されるため、別のブラウザやスマホからでも同じアカウントで
  ログインすれば同じデータが見えます**。ただし配色テーマ・通知ON/OFF設定は個人のUI設定として
  引き続き`localStorage`保存のため、この2つだけは端末・ブラウザごとに独立したままです。
- **ゲスト（匿名）アカウントのログアウト時、ブラウザの開発者ツールに403エラーのネットワークログが
  表示されることがある**：`delete_own_account()`で自分自身の`auth.users`行を削除した直後に
  `signOut()`を呼ぶと、Supabase Auth側が「JWTのsubクレームに対応するユーザーが見つからない」と
  判断し、ログアウトのAPIリクエスト自体が失敗する（既知のSupabase Auth側の挙動）。アプリの
  動作としては実害がなく（ログイン画面に正しく戻り、データも正しく削除される）、ブラウザが
  自動的に表示するネットワークエラーログのため、アプリのコード側で完全に消すことはできない
  （認証・DB設計書.md12章参照）。
- **放置されたゲスト（匿名）アカウントの定期削除は未着手**：ゲストアカウントはログアウト時に
  自動削除される設計ですが、ログアウトせずブラウザを閉じただけの場合はDB上に残り続けます。
  一定期間操作のない匿名アカウントを定期的に一括削除する仕組み（バッチ処理等）は、YAGNI判断で
  今回は見送りました（認証・DB設計書.md12.6参照）。
- ✅**（2026-09-02修正済み）`task_assignees` / `task_subtasks`のRLSポリシーがプロジェクト単位に
  未対応だった問題**：プロジェクト管理機能導入時に`tasks`本体のRLSポリシーは
  「プロジェクトメンバーのみ」に更新されたが、担当者中間テーブル`task_assignees`とサブタスク
  テーブル`task_subtasks`は当初のまま（ログイン済みなら誰でも閲覧・追加・削除可）で、
  プロジェクトメンバーかどうかのチェックが漏れていた。`is_task_project_member(task_id)`
  ヘルパー関数を新設し、`tasks`と同じ方式に修正（`supabase-migration-task-assignees-subtasks-rls-fix.sql`）。
  詳細は`認証・DB設計書.md`11.5.1③を参照。
- ✅**（2026-09-02修正済み）ダッシュボードの「メンバー別稼働状況」（`ProgressChart.tsx`）が、
  選択中プロジェクトのメンバーに絞られず、DB上の全ユーザー（＝`profiles`テーブルの全行）を
  表示していた問題**：`App.tsx`の`users`状態は`supabase.from('profiles').select(...)`で
  無条件に全件取得しており（5章の「ログイン済みなら全員閲覧可」というprofilesのRLS方針自体は
  意図通り）、`DashboardView` → `ProgressChart`にそのまま渡していた（`GanttChart.tsx`も同じ
  `users`propを受け取るが、表示対象タスク自体が既にプロジェクト絞り込み済みのため実害は
  無かった）。TaskForm.tsxの担当者・確認者候補と同じ`projectMembers`ベースの絞り込みパターンで
  `App.tsx`に`currentProjectMembers`を追加し、`DashboardView`へ渡す`users`をこれに差し替えて
  修正した。

---

## 9. 開発ルール（`規約.md`の要点）

このプロジェクトには `規約.md` という、コード修正時に必ず守るべきルールがあります。要点だけ抜粋します。

1. **状態はApp.tsxに集約する**（Single Source of Truth）。子コンポーネントが直接データを書き換えることは禁止。
2. **外部ルーティングライブラリを使わない**（`currentView`の文字列切り替えのみで画面遷移する）。
3. **遅延最優先ルール**：期日超過のタスクは、どんな状態でも赤い「遅延」表示が最優先。
4. 型定義（`types/task.ts`）を安易に緩めない。`any`型で逃げない。
5. デザインは `index.css` のCSS変数のみを使う。派手な装飾や絵文字を追加しない。
6. コード修正の提案は、ファイル全体ではなく「どこをどう直すか」のピンポイントな差分で行う。
7. 機能を増やすより、不要なコードを削る「引き算」を優先する。

---

以上が現時点の仕様です。今後機能を追加・変更した際は、このドキュメントも合わせて更新することをおすすめします。

React／TypeScript／Supabaseなどの用語や技術的な背景から学びたい場合は、`学習ノート.md`を
参照してください。
