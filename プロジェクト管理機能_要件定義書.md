# WORK PLUS「プロジェクト管理」機能 要件定義書

この文書は、サイドバーに項目だけ存在し「近日公開」のまま未実装になっている「プロジェクト管理」を
実装するにあたり、実装に着手する前に要件・データモデル・権限設計を先に固めておくための文書です。
`認証・DB設計書.md`と同じ位置づけで、実装後は10章のような形で差分を追記していく想定です。

作成日：2026-08-29
更新日：2026-08-30（8章の未確定事項6点について回答をもらい、方針を確定。あわせて
2章のUI/UX要件を具体化し、サイドバーのアコーディオン方式によるプロジェクト切り替え、
タスクのプロジェクト間移動、オーナー譲渡を伴う退会フローを反映）
更新日：2026-08-31（プロジェクトのステータスを、2値案ではなく最初に提示した3値案
「進行中／完了／アーカイブ」で確定するよう2.2・3.1を修正）

ステータス：**要件定義済み（未実装）**。1〜8章の方針が確定。9章の実装ステップに沿って
着手できる状態

---

## 0. 現状とスコープ

- サイドバーの4項目（ダッシュボード／スケジュール／プロジェクト管理／タスク一覧）のうち、
  「プロジェクト管理」だけがクリックすると「近日公開」というプレースホルダーが出るのみで、
  中身は未実装（`仕様書.md`6.3・8章）
- 現状のタスクは「1つの共有掲示板」モデルです。ログインしている全員が同じ`tasks`テーブルを
  共有し、担当者・カテゴリ・優先度で絞り込んで見ているだけで、「プロジェクトごとに分ける」
  という概念自体がまだ存在しません
- **本機能のスコープ（確定）**：本格的な複数プロジェクト管理を導入します。「プロジェクト」を
  新しいデータの単位として設け、タスクは必ずどれか1つのプロジェクトに属するようにします。
  ダッシュボード・カンバン・スケジュールの3画面も、プロジェクトごとに切り替えて見られるように
  します（詳細は2章）

---

## 1. 全体イメージ

```
【現状】
ログイン中のユーザー全員 ── 1つの共有tasksテーブル（プロジェクトの区別なし）

【導入後】
ログイン中のユーザー
  └─ 自分が所属するプロジェクト一覧（projects × project_members）
        └─ プロジェクトA を選択中 ── ダッシュボード／タスクボード／スケジュールは
                                        すべて「プロジェクトAのタスクだけ」を表示
        └─ プロジェクトB を選択中 ── 同様にプロジェクトBのタスクだけを表示
```

ポイントは、**ダッシュボード・タスクボード・スケジュールの3画面を作り直すのではなく、
「今どのプロジェクトを見ているか」という状態を1つ追加し、既存の3画面をそのプロジェクトで
絞り込んで表示するだけにする**ことです。`filterUser` / `filterCategory` / `filterPriority`と
同じように、`currentProjectId`という状態をApp.tsxに1つ追加し、Supabaseへの問い合わせに
`.eq('project_id', currentProjectId)`を足すだけで済むようにします。これにより、
既存のKanbanBoard.tsx / DashboardView.tsx / ScheduleView.tsxはほぼ改修不要です。

---

## 2. UI/UX要件

「プロジェクト管理のUI/UXはどうなるか」について、確定した方針をまとめます。大きく分けて
**(A) サイドバーからのプロジェクト切り替え**と、**(B) プロジェクト管理タブでの一覧・作成・
メンバー管理**の2つの動線があります。

### 2.1 (A) サイドバーのアコーディオン式プロジェクト切り替え（確定）

ヘッダーに別途スイッチャーを新設する案もありましたが、**サイドバーの「プロジェクト管理」項目
自体をアコーディオンにする**方針で確定しました。既存のメニュー項目（ダッシュボード／
スケジュール／プロジェクト管理／タスク一覧）はそのままに、「プロジェクト管理」だけクリックで
開閉するようにします。

```
サイドバー（閉じた状態）              サイドバー（「プロジェクト管理」を開いた状態）
┌────────────────────┐        ┌────────────────────┐
│ 🏠 ダッシュボード         │        │ 🏠 ダッシュボード         │
│ 📅 スケジュール           │        │ 📅 スケジュール           │
│ 📁 プロジェクト管理  ▾    │        │ 📁 プロジェクト管理  ▴    │
│ ✅ タスク一覧             │        │   ● プロジェクトA  ← 選択中│
│                        │        │     プロジェクトB         │
│                        │        │     プロジェクトC         │
│                        │        │     ＋ 新規プロジェクト     │
│                        │        │     すべて管理 →           │
│                        │        │ ✅ タスク一覧             │
└────────────────────┘        └────────────────────┘
```

- 「プロジェクト管理」をクリックするとアコーディオンが開閉し、自分が参加しているプロジェクトが
  一覧表示される（他のメニュー項目のように別画面へ即座に遷移するのではなく、その場で展開する）
- プロジェクト名をクリックすると、それが「現在のプロジェクト（`currentProjectId`）」になり、
  自動的にダッシュボードへ遷移する。以降、ダッシュボード／タスクボード／スケジュールの3タブは
  すべてこのプロジェクトの内容を表示する
- 選択中のプロジェクトには●印などで現在地を示す
- 一覧の下に「＋新規プロジェクト」（2.2のモーダルを開く）と「すべて管理→」
  （2.3の一覧・メンバー管理ページへ遷移する）を常設する
- 選択状態は`theme`と同じ扱いで`localStorage`に保存し、リロードしても直前に見ていた
  プロジェクトを復元する
- サイドバーが折りたたみ状態（アイコンのみ表示）のときは、アコーディオンではなく
  クリックで2.3のプロジェクト管理ページへ直接遷移する簡易的な挙動にする（折りたたみ幅では
  展開表示するスペースが無いため）
- ダッシュボード／タスクボード／スケジュールの3タブは、現在のプロジェクトが未選択の状態
  （初回ログイン時など）では「プロジェクトを選択してください」という案内を出し、内容は
  表示しない

### 2.2 プロジェクト作成・編集モーダル

- 入力項目：プロジェクト名（必須）／説明（任意）／ステータス
- **ステータスは「進行中」「完了」「アーカイブ」の3値で確定**しました（最初に提示した案の
  とおり。「完了」は作業自体は終わったがプロジェクトとしては残しておきたい状態、
  「アーカイブ」は一覧から隠したい過去のプロジェクトを示す状態、という役割分担です。
  サイドバーのアコーディオンや一覧では**アーカイブのみ**デフォルト非表示にし（完了は
  通常通り表示する）、一覧側に「アーカイブ済みを表示」の切り替えを設ける想定）
- 作成すると、作成者が自動的に`project_members`へ「オーナー」として登録される
  （タスクの`createdBy`と同じ考え方）
- 編集はオーナーのみ可能（7章）

### 2.3 (B) プロジェクト管理タブ（プロジェクト一覧・メンバー管理ページ）

サイドバーのアコーディオンが「素早い切り替え」用の動線だとすると、こちらは「一覧性・管理操作」
のための画面です。サイドバーの「プロジェクト管理」から見ると、アコーディオンの「すべて管理→」
経由でも到達できます。

```
┌───────────────────────────────────────────┐
│ プロジェクト管理                    ［＋ 新規プロジェクト］ │
├───────────────────────────────────────────┤
│ ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│ │ プロジェクトA      │ │ プロジェクトB      │ │ プロジェクトC    │ │
│ │ オーナー：あなた    │ │ メンバー           │ │ オーナー：あなた  │ │
│ │ 👤👤👤 +2人       │ │ 👤👤 2人           │ │ 👤 1人          │ │
│ │ 進捗 ████░░ 62%   │ │ 進捗 █░░░░░ 12%   │ │ 進捗 ██████ 100%│ │
│ │        ［開く］    │ │        ［開く］    │ │      ［開く］    │ │
│ └─────────────┘ └─────────────┘ └───────────┘ │
└───────────────────────────────────────────┘
```

- 自分が参加している（`project_members`に自分の行がある）プロジェクトをカード形式で並べる
- 各カードに表示する情報：プロジェクト名／自分のロール（オーナー・メンバー）／メンバー数
  （アバターを重ねて数人分表示、他は「+n」）／タスク件数・完了率の簡易サマリー／
  「開く」ボタン（クリックで2.1の「現在のプロジェクト」を切り替え、ダッシュボードへ遷移）
- カード右上（オーナーのみ）に「編集」「メンバー管理」「削除」のメニューを表示
- 参加プロジェクトが0件のとき（新規ユーザー等）は空状態の案内＋作成ボタンを大きく表示

### 2.4 メンバー管理

- プロジェクトカードの「メンバー管理」から、既存の登録ユーザー（`profiles`）の中から選んで
  メンバーを追加できる（担当者選択と同じUI・体験を流用）
- オーナーはメンバーの削除、他メンバーへの「オーナー譲渡」ができる
- メンバー自身が「このプロジェクトから抜ける」こともできる。オーナー本人は、他の誰かに
  オーナーを譲渡するまで抜けられない（7.4・6.3参照）

### 2.5 タスクのプロジェクト間移動（確定：対応する）

`TaskForm.tsx`の編集画面に「プロジェクト」欄を追加し、自分が参加している他のプロジェクトへ
タスクを移動できるようにします。担当者・確認者と同様、移動先プロジェクトのメンバーで
なくなる担当者・確認者がいる場合の扱いは、実装時に「担当者・確認者もリセットする」
シンプルな仕様にする想定です（8.3参照）。

---

## 3. データモデル設計

### 3.1 `projects`（プロジェクト本体）

```sql
create table projects (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  description text,
  status text not null default 'active' check (status in ('active', 'completed', 'archived')),
  created_by uuid references profiles(id) not null,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);
```

### 3.2 `project_members`（プロジェクトとメンバーの中間テーブル）

```sql
create table project_members (
  project_id uuid references projects(id) on delete cascade,
  user_id uuid references profiles(id) on delete cascade,
  role text not null default 'member' check (role in ('owner', 'member')),
  joined_at timestamptz default now(),
  primary key (project_id, user_id)
);
```

`task_assignees`（認証DB設計書4.3）と同じ「中間テーブルで多対多を表す」パターンです。

### 3.3 `tasks`への追加

```sql
alter table tasks add column project_id uuid references projects(id);
-- 移行完了後、not nullに変更する（3.4参照）
```

### 3.4 既存タスクの移行方針

現在Supabase上にある既存タスクは、まだプロジェクトに属していません。マイグレーション実行時に
以下を自動で行う想定です。

1. 「（移行前タスク）」のような名前で、現在の全ユーザーをメンバーとする既定プロジェクトを
   1つ自動作成する
2. 既存の全タスクの`project_id`をそのプロジェクトのIDで一括更新する
3. その後`tasks.project_id`に`not null`制約を付け、以降のタスク作成では必ずプロジェクトが
   必須になるようにする

移行後にプロジェクト名の変更や、タスクの別プロジェクトへの移動（2.5）ができるため、
ユーザー側で後から整理し直せます。

---

## 4. ダッシュボード・タスクボード・スケジュールへの影響

- 3画面とも、Supabaseへの問い合わせに`project_id = currentProjectId`の絞り込みを追加するだけ
- フィルターバー（担当者・カテゴリ・優先度）は現状通り「選択中プロジェクト内での」絞り込みとして
  そのまま使える
- **通知ベルの範囲は「自分が参加している全プロジェクトを横断して通知する」で確定**しました。
  選択中プロジェクトだけに絞ると、自分が今見ていない別プロジェクトの承認待ちなどに気づけず
  本末転倒になるためです。通知アイテムをクリックした際、そのタスクが選択中でない別プロジェクトの
  ものであれば、`currentProjectId`をそのプロジェクトへ自動的に切り替えてから編集モーダルを
  開く挙動にします
- サイドバーの「タスク一覧」タブは、これまで通り現在のプロジェクトのカンバンボードを表示する
  役割になります（呼び名が紛らわしくなるため、「プロジェクト管理」＝一覧・切り替え・メンバー管理、
  「タスク一覧」＝選択中プロジェクトのカンバン、という役割分担を明確にする）

---

## 5. サンプルデータ・退会機能への影響

- **設定ページの「サンプルデータへのリセット」は「いったんこれまで通り」で確定**しました。
  プロジェクト単位への変更は見送り、現状通り「全プロジェクト横断で、自分が作成したタスクを
  削除して初期サンプルへ戻す」挙動のままにします（プロジェクトを跨いだサンプルタスクが
  作られる形になりますが、そもそも開発中の動作確認用の機能のため実害は小さいと判断）
- 退会（アカウント削除）機能への影響は、6章で詳しく扱います（オーナー権限の引き継ぎが必要）

---

## 6. 退会（アカウント削除）とオーナー権限の引き継ぎ（確定）

「退会するときにオーナー権限を指定した人に渡せるのが一番よい」という方針で確定しました。
既存の`delete_own_account`関数（`認証・DB設計書.md`4.7）をそのまま拡張するのではなく、
**オーナー譲渡を独立した機能として先に用意し、退会フローの中でそれを利用する**設計にします
（2.4で触れた「オーナーがメンバーへオーナー権限を譲渡する」通常の機能と、退会時の処理を
同じ仕組みで実現できるため、二重に作らずに済みます）。

### 6.1 オーナー譲渡用の関数（新規）

```sql
create or replace function public.transfer_project_ownership(p_project_id uuid, p_new_owner_id uuid)
returns void
language plpgsql
security definer
set search_path = public
as $$
begin
  -- 呼び出し本人が、このプロジェクトの現オーナーであることを確認
  if not exists (
    select 1 from project_members
    where project_id = p_project_id and user_id = auth.uid() and role = 'owner'
  ) then
    raise exception 'オーナーのみが実行できます';
  end if;

  -- 譲渡先が、既にこのプロジェクトのメンバーであることを確認
  if not exists (
    select 1 from project_members
    where project_id = p_project_id and user_id = p_new_owner_id
  ) then
    raise exception '譲渡先はこのプロジェクトのメンバーである必要があります';
  end if;

  update project_members set role = 'member' where project_id = p_project_id and user_id = auth.uid();
  update project_members set role = 'owner' where project_id = p_project_id and user_id = p_new_owner_id;
end;
$$;

grant execute on function public.transfer_project_ownership(uuid, uuid) to authenticated;
```

### 6.2 退会フロー（フロントエンド側の流れ）

1. 退会ボタンを押すと、まず「自分がオーナーで、かつ他にメンバーがいるプロジェクト」を
   （フロント側で`project_members`を参照して）調べる
2. 該当プロジェクトが1件以上あれば、退会確認ダイアログの前に「引き継ぎが必要なプロジェクトが
   あります」という画面を挟み、プロジェクトごとに新しいオーナーを選択してもらう
   （担当者・確認者選択と同じUIを流用）
3. 選択された組み合わせぶんだけ`transfer_project_ownership`を呼び出し、すべて成功したら
   通常の退会確認（パスワード再入力＋`window.confirm`）に進み、`delete_own_account`を呼ぶ
4. **自分がオーナーで、かつ他にメンバーがいないプロジェクト**（自分1人だけのプロジェクト）は
   引き継ぎ先が存在しないため、退会と同時にそのプロジェクトごと削除する（自分が単独作成した
   タスクが退会時に削除されるのと同じ考え方）。`delete_own_account`関数側で
   `delete from projects where id in (select project_id from project_members where user_id = auth.uid() and role = 'owner')`
   のような処理を追加し、`on delete cascade`で`project_members`・`tasks`も連動して片付くようにする

---

## 7. 権限（ロール）設計

### 7.1 RLS方針

- **`projects`**：閲覧はメンバーのみ／作成はログイン済みなら誰でも／更新・削除はオーナーのみ
- **`project_members`**：閲覧はメンバーのみ／追加・削除・ロール変更はオーナーのみ
  （自分自身の脱退のみ本人にも許可）
- **`tasks`**：既存ポリシー（閲覧・作成・更新はログイン済みなら誰でも、削除は作成者のみ）に
  「かつ、そのタスクのプロジェクトのメンバーであること」という条件を追加する

### 7.2 今回実装する権限まとめ

| 操作 | オーナー | メンバー | 非メンバー |
|---|---|---|---|
| プロジェクトの閲覧 | ○ | ○ | × |
| プロジェクト内タスクの閲覧・作成・更新 | ○ | ○ | × |
| プロジェクト内タスクの削除 | ○（自分の作成分） | ○（自分の作成分） | × |
| プロジェクト名・説明・ステータスの編集 | ○ | × | × |
| メンバーの追加・削除 | ○ | × | × |
| プロジェクトの削除 | ○ | × | × |
| オーナー譲渡（6.1） | ○ | × | × |

### 7.3 組織全体の管理者ロールについて（TODO・今回は実装しない）

組織全体の管理者ロールは、実は`profiles`に`app_role`列を1つ足し、既存のRLSポリシーへ
「または管理者なら許可」というOR条件を追加するだけで後乗せできる、スキーマを作り直す必要の
ない拡張です。

```sql
-- 将来追加する場合のイメージ（今回は実装しない）
alter table profiles add column app_role text not null default 'member' check (app_role in ('admin', 'member'));
```

このアプリには現状「組織」という概念自体が無く、管理者が実際に何を管理するのかがまだ
定義できていないため、今回は見送り、必要になった時点で改めて設計します。

### 7.4 オーナーが1人しかいない場合の扱い

- 通常時：オーナーは、他のメンバーにオーナーを譲渡（6.1）するまでプロジェクトから
  自主的に脱退できない
- 退会時：自分1人だけのプロジェクトはプロジェクトごと削除、他にメンバーがいる場合は
  退会前に譲渡が必須（6章）

---

## 8. まだ細部が決まっていない論点（実装しながら決めても問題ない粒度）

大枠（1〜7章）は確定しましたが、実装の過程で以下の細部は都度決めていきます。

1. **プロジェクトカードの完了率の計算方法**：カンバンの`done`ステータスの割合で単純計算するか、
   `KpiCards.tsx`と同じロジックを流用するか
2. **アーカイブ済みプロジェクトの扱い**：サイドバーのアコーディオン・一覧ページの両方で
   デフォルト非表示にする方針は確定したが、「表示する」トグルをどちらの画面に置くか
3. **タスク移動時に、移動先プロジェクトのメンバーでなくなる担当者・確認者の扱い**：
   自動でクリアする方式で仮決めしているが、実装時に警告表示を出すかどうか
4. **メンバーが0人になったプロジェクト**（オーナー譲渡後、元オーナーも脱退した結果など）を
   自動アーカイブするかどうか

---

## 9. 段階的な実装ステップ（案）

1. `projects` / `project_members`テーブル・トリガー（プロジェクト作成時に自動オーナー登録）・
   `transfer_project_ownership`関数・RLSポリシーを追加する`supabase-migration-projects.sql`を作成
2. `tasks.project_id`列を追加し、既存タスクを既定プロジェクトへ移行するマイグレーションを実行
3. サイドバーの「プロジェクト管理」をアコーディオン化し（2.1）、`currentProjectId`をApp.tsxに
   追加。ダッシュボード／タスクボード／スケジュールの問い合わせに`project_id`の絞り込みを追加
4. プロジェクト管理タブ（一覧・作成・編集。2.2〜2.3）を実装
5. メンバー管理・オーナー譲渡UI（2.4・6.1）を実装
6. `TaskForm.tsx`にプロジェクト移動欄を追加（2.5）
7. 退会フローにオーナー引き継ぎのステップを追加（6.2）
8. 通知ベルを全プロジェクト横断に対応（4章）
9. 動作確認・仕様書／認証DB設計書への反映
10. （将来・TODO）組織全体の管理者ロール（7.3参照）が本当に必要になったタイミングで着手

---

以上が「プロジェクト管理」機能の要件定義です。この内容で着手してよければ、9章のステップ1
（`supabase-migration-projects.sql`の作成）から進めます。
