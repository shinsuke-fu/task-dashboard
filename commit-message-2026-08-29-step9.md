# コミットメッセージ案（2026-08-29・ステップ9：ドキュメント反映）

前回のコミット（ステップ8＋通知UI改善＋レイアウト崩れ修正）以降の変更分です。今回も前回コミットが
存在するため、通常通り`git add`→`git commit`で問題ありません。

（このファイル自体はGitにコミットしない、チャット経由で渡すだけの補助ファイルです）

---

## コミットメッセージ

```
docs: プロジェクト管理機能を仕様書・認証DB設計書に反映

- 仕様書.md：1章（アプリ概要）・3章（ディレクトリ構成）・4章（状態管理）・
  5章（データモデル：Project型・projectId追加）・6章（通知ベルの全プロジェクト横断対応・
  サイドバーの並び順変更・TaskFormのプロジェクト移動・設定ページのオーナー引き継ぎ・
  タブレットレイアウト崩れ修正・6.11「プロジェクト管理機能」節を新設）・7章（ファイル一覧）・
  8章（TODO：実退会テスト保留、タスク内コメント機能構想などを追記）・changelogを更新
- 認証・DB設計書.md：新規11章「プロジェクト管理機能」を追加。projects/project_members
  テーブル、tasks.project_id、RLS設計、実装時に発生した2件のRLS不具合
  （再帰的RLS参照／INSERT直後のRETURNINGとの鶏と卵問題）とその対処、
  transfer_project_ownership関数、delete_own_accountの拡張をまとめて記録。
  0章の「プロジェクト管理は対象外」という記述を、実装済みである旨に更新
```

---

## 補足

- コード側の変更はありません（ドキュメントのみ）。
- `git add` / `git commit` / `git push`はユーザー側で実行してください
  （`feature/project-management`ブランチ上）。
